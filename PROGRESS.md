# PROGRESS — ASSISTANT AJE

## 2026-08-05

- [x] Transcription de la note vocale (discussion Tobie / père) via `faster-whisper` local, corrigée et complétée par Tobie.
- [x] Analyse du besoin réel : secrétariat numérique perso pour magistrat (dossiers, audiences, tâches, rappels intelligents, bilan mensuel, confidentialité stricte).
- [x] Recherche de solutions existantes : voir [Notes/2026-08-05-recherche-solutions-existantes.md](Notes/2026-08-05-recherche-solutions-existantes.md).
  - Aucune solution existante ne couvre le besoin exact (institutionnel, cabinet d'avocat, IA générique, ou service citoyen béninois — mais rien de perso+judiciaire+confidentiel).
- [x] Choix de Tobie : rédiger un cahier des charges détaillé avant de coder.
- [x] Grille de questions v1 préparée — jugée trop technique/abstraite par Tobie, corrigée.
- [x] Grille de questions v2 (langage simple, questions sur le métier/les galères réelles) : voir [Notes/2026-08-05-grille-interview-pere.md](Notes/2026-08-05-grille-interview-pere.md).
- [x] Interview approfondie réalisée par Tobie avec son père — besoin réel identifié avec précision.
- [x] Recherche ciblée v2 effectuée : voir [Notes/2026-08-06-recherche-ciblee-v2.md](Notes/2026-08-06-recherche-ciblee-v2.md).
  - Le besoin = 3 mécanismes précis : (1) échéances en cascade 2 semaines avant une audience, (2) relance/nagging jusqu'à confirmation "c'est fait", (3) bilan hebdo avec report des tâches non faites.
  - Chaque mécanisme existe séparément ailleurs (Clio/PracticePanther pour le 1, NagMe/Todof pour le 2, Sunsama pour le 3) mais **aucun outil ne combine les 3** dans un contexte judiciaire confidentiel adapté au Bénin.
  - Conclusion : pas de solution toute faite, mais le projet est plus petit que prévu (un moteur de rappels + bilan, pas un logiciel de gestion de tribunal complet).
- [x] Tobie a choisi de trancher la confidentialité en premier (avant l'outil).
- [x] Recherche du cadre légal béninois : voir [Notes/2026-08-06-confidentialite-donnees.md](Notes/2026-08-06-confidentialite-donnees.md).
  - Loi n°2017-20 (Code du numérique, Livre 5) + APDP. Les données judiciaires du père (poursuites/procédures) rentrent probablement dans la catégorie "données sensibles".
  - ⚠️ Point à faire confirmer par le père (accès à un collègue juriste) : obligations exactes de déclaration APDP pour cet usage précis.
  - Recommandation prudente : commencer 100% en local (rien en ligne), le temps de clarifier le besoin multi-appareils.
- [x] Tobie propose : cloud simple (Firebase/Supabase) + noms de code choisis par le père au lieu des vraies infos.
- [x] Red Team de cette idée : voir [Notes/2026-08-06-redteam-anonymisation.md](Notes/2026-08-06-redteam-anonymisation.md).
  - Bon principe (minimisation des données) mais 3 trous : métadonnées qui parlent, où stocker la correspondance nom de code↔vraie affaire, et surtout le facteur humain (un seul oubli = fuite définitive).
  - Renforcement proposé : ajouter un chiffrement côté téléphone (seul le père a la clé) en plus des noms de code → comble les 3 trous.
- [x] Décision prise : Option 2 — noms de code choisis par le père + chiffrement côté téléphone avant tout envoi au cloud (Firebase/Supabase).

## Décisions d'architecture actées (2026-08-06)
1. Besoin = moteur de rappels (pas un logiciel de gestion de dossiers complet) avec 3 mécanismes : échéances en cascade à 2 semaines, relance jusqu'à confirmation, bilan hebdo avec report.
2. Aucune solution existante ne combine les 3 dans un contexte judiciaire confidentiel → projet justifié.
3. Confidentialité : cloud simple (Firebase ou Supabase) + le père utilise des noms de code pour les dossiers + chiffrement fait sur son téléphone avant tout envoi (le cloud ne voit jamais les données en clair).

## 2026-08-07 — Corrections et cadrage du périmètre MVP
- [x] Correction : la relance se fait **chaque jour** (pas chaque semaine) tant que la tâche n'est pas confirmée faite. Voir [Notes/2026-08-06-recherche-ciblee-v2.md](Notes/2026-08-06-recherche-ciblee-v2.md).
- [x] Clarifié : l'app fait du **suivi d'échéances** (quoi + quand ça tombe), pas de la **planification horaire** (quand dans la journée). Le père garde la main sur les priorités. Voir [Notes/2026-08-07-echeances-vs-planification.md](Notes/2026-08-07-echeances-vs-planification.md).
- [x] Décision : le MVP reste sur le périmètre échéances + relance quotidienne + bilan hebdo. Pas de planification horaire pour l'instant (ajout possible plus tard si le besoin se confirme).

- [x] Firebase retenu (vs Supabase) : FCM natif pour la relance quotidienne + Firestore offline. Voir [Notes/2026-08-07-firebase-vs-supabase.md](Notes/2026-08-07-firebase-vs-supabase.md).
- [x] MVP v1 cadré, puis Red Team round 1 (Tobie) → gaps A/B/C/D trouvés (notif par item, chiffrement cassé par la notif, ajout un par un, pas de niveau Dossier).
- [x] MVP v2 : corrige A/B/C/D. Red Team round 2 (moi, sur demande de Tobie) → gaps H/I/J trouvés (push silencieux peu fiable sur iPhone, pas de flux "modifier une date", pas de verrou d'app).
- [x] MVP v3 : corrige H/I/J.
- [x] Clarification de rôle (mémorisée) : c'est Claude qui fait les Red Team, Tobie décide/manage — pas l'inverse.
- [x] Red Team round 3 (Claude, sur les 5 questions de Tobie) → gaps N/O/P/Q/T trouvés (pas de statut "en retard", ajout groupé incomplet, clé de chiffrement liée au device = perte si téléphone perdu, empilement de notifs hors-ligne, écran de relecture trop lourd). Voir [Notes/2026-08-07-redteam-mvp-v3.md](Notes/2026-08-07-redteam-mvp-v3.md).
- [x] MVP v4 : corrige N/O/P/Q/T. Voir [Notes/2026-08-07-mvp-scope.md](Notes/2026-08-07-mvp-scope.md) (périmètre à jour et complet).

- [x] Tobie a validé le MVP v4 → début du développement.

## 2026-08-07 — Développement (Flutter + Firebase)
- [x] Projet Flutter créé (`app/`, org `bj.pentalightcode`, nom `secretaire_aje`), cible Android + iOS + Web.
- [x] Projet Firebase `secretaire-aje` créé, Firestore (Standard, région `eur3` Europe), Hosting (`secretaire-aje.web.app`).
- [x] Authentification : lien magique par email (sans mot de passe) + Google Sign-In en option. Migration nécessaire car Firebase Dynamic Links est mort (août 2025) → mis en place via Firebase Hosting + intent-filter Android (`assetlinks.json`).
- [x] Bug trouvé et corrigé : le lien initial était perdu (émis dans un flux avant que quiconque l'écoute). Vérifié fonctionnel de bout en bout (test manuel réussi : "Connecté ✔").
- [x] Piège de cache navigateur identifié et corrigé à la source : les fichiers `.js/.html/.json` de Hosting étaient mis en cache 1h (`max-age=3600`), causant une confusion majeure (anciennes versions affichées). Corrigé avec `Cache-Control: no-cache, must-revalidate` dans `firebase.json`. **Point important pour la suite : si un truc a l'air de ne pas marcher, toujours vérifier via `curl` ou `fetch(cache:'no-store')` avant de suspecter le code.**
- [x] Verrou d'app (PIN + biométrie si dispo) — `lock_service.dart`, `creer_pin_screen.dart`, `verrou_screen.dart`.
- [x] Modèle de données Dossier/Tâche (`models/`) + règles de sécurité Firestore strictes (accès limité au propriétaire, point S).
- [x] Écran "Aujourd'hui" (groupé par dossier, tri par urgence, "En retard" mis en évidence — point O) et écran "Ajouter" (version unitaire) — `screens/aujourdhui_screen.dart`, `screens/ajouter_screen.dart`.
- [x] `creerDossierAvecTache()` crée automatiquement une tâche par défaut avec un dossier (point P), `modifierDateAudience()` recalcule les rappels liés (point I).

- [x] Réinitialisation du code PIN oublié : bouton "Code PIN oublié ?" sur l'écran de verrou → repasse par le lien magique (email) pour prouver l'identité, efface le PIN, recréation d'un nouveau au retour. Les dossiers/tâches ne sont jamais affectés (liés au compte, pas au PIN).

- [x] Écran "Bilan de la semaine" : groupé par dossier, 3 compteurs (Fait sur 7 jours / En retard / À venir). Ajout du champ `dateFait` sur les tâches pour savoir ce qui a été fait récemment.

- [x] Ajout groupé : coller le "rôle" hebdomadaire (une ligne par dossier, format `Nom | jj/mm/aaaa`), relecture légère (compte les lignes valides/ignorées) puis une seule confirmation — `ajout_groupe_screen.dart`, `utils/parseur_role.dart`. Accessible depuis un bouton sur l'écran "Ajouter".

## 2026-08-07 — Phase 3 (onboarding, natures personnalisées, Profil)
- [x] Remplacement complet de l'ancien `TypeTache` (4 types fixes) par des **natures de dossier dynamiques par utilisateur** (`NatureDossier`), stockées dans `/utilisateurs/{uid}/naturesDossier`.
- [x] Écran d'accueil (`onboarding_screen.dart`) : prénom, pseudo, profession, délai de rappel. Pré-remplit les natures selon la profession (Magistrat/Avocat/Notaire-Huissier/Médecin/Professeur/Entrepreneur/Autre) — voir `models/profession.dart`.
- [x] Gestion des natures personnalisées (ajout/suppression, "rappel immédiat" ou "délai standard") — panneau partagé entre "Ajouter" et "Profil" (`gestion_natures_sheet.dart`).
- [x] Écran Profil : modifier pseudo/délai de rappel, gérer ses natures, **déconnexion** (double confirmation), **suppression de compte** (triple confirmation, 3 messages distincts, supprime toutes les données + le compte Firebase).
- [x] 4ᵉ onglet de navigation "Profil" ajouté.
- [x] Testé de bout en bout : onboarding → natures pré-remplies pour "Magistrat" confirmées visuellement.

## 2026-08-07 — Corrections après remarques de Tobie sur la Phase 3
- [x] Profession personnalisable : si "Autre", un champ texte libre permet de préciser sa profession (à l'inscription ET modifiable ensuite dans le Profil).
- [x] Retrait du bilan "Jour" (trop court pour être utile).
- [x] Terminologie : "nature de dossier" → **"type de dossier"** dans tous les textes visibles (le nom technique interne reste `NatureDossier` dans le code, sans impact pour l'utilisateur).
- [x] Texte du "rappel immédiat" clarifié : explique maintenant explicitement que ça change seulement le *moment où ça commence* (dès l'ajout, pas d'attente), pas la fréquence (toujours relancé chaque jour comme le reste).
- [x] Décision actée pour plus tard (Phase 4, notifications) : deux rappels quotidiens (matin/soir) au lieu d'un seul — heures fixes approximatives (~7h/~19h) plutôt qu'un vrai calcul astronomique lever/coucher du soleil (trop complexe pour le bénéfice, surtout que ces horaires varient peu près de l'équateur).

## 2026-08-08 — Corrections après tests réels (bug de date, bilan, professions)
- [x] **Vrai bug corrigé** : le calcul "Échéance demain/dans X jours" comparait l'heure exacte du moment à minuit de la date cible, décalant tout d'un jour selon l'heure de la journée (trouvé par Tobie : le 8 août, "10 août" affichait "demain" au lieu de "dans 2 jours"). Corrigé en comparant des dates calendaires pures.
- [x] **Liste des professions élargie et diversifiée** : passée de 6 (dont 3 juridiques) à 12, couvrant santé, éducation, droit, finance, conseil, commerce, BTP, immobilier, artisanat, beauté/bien-être, entrepreneuriat — plus plausible pour un vrai produit multi-métiers.
- [x] **Bilan repensé en profondeur** : navigation par flèches ◀▶ à travers les périodes passées ET futures (calendaires réelles : semaine ISO, mois, trimestre, semestre, année civile) au lieu d'une fenêtre glissante fixe de N jours. Corrige aussi un vrai besoin découvert par Tobie : les dossiers "en attente" (délai standard pas encore ouvert, donc invisibles dans "À traiter") redeviennent visibles en avançant le Bilan vers leur période future — sans ça, risque d'oubli et de doublons.
- [x] "En retard" n'est affiché que sur la période en cours (n'a pas de sens pour du passé/futur).
- [x] Explication donnée à Tobie sur le "suivi immédiat" vs "délai standard" : différence invisible si l'échéance de test est plus proche que le délai standard lui-même (pas un bug, un test qui ne pouvait pas révéler la différence).

## 2026-08-09 — Nom et icône définitifs : l'app s'appelle désormais "Vigie"
- [x] Nom choisi après plusieurs itérations : Veillance → Confident → fusion "Veillance" → puis **"Vigie"** (le guetteur qui anticipe, veille, et à qui on fait confiance — un seul mot réel plutôt qu'un néologisme forcé).
- [x] Icône créée sur mesure (Python/PIL) : croissant de lune doré sur fond bleu nuit (la veille), avec un trou de serrure découpé dedans (la confidentialité) et une coche de validation dans le creux sombre (les tâches) — les 3 piliers de l'app dans un seul symbole, itéré avec Tobie jusqu'à validation.
- [x] Nom et icône appliqués partout : AndroidManifest.xml, Info.plist (iOS), web/manifest.json, web/index.html, `MaterialApp.title`, écran de connexion, texte du verrou biométrique. Les identifiants techniques (`bj.pentalightcode.secretaire_aje`) restent inchangés volontairement (liés à toute la config Firebase Auth/App Links) — changement cosmétique seulement, pas de risque sur ce qui fonctionne déjà.
- [x] Icônes générées automatiquement pour Android/iOS/Web via `flutter_launcher_icons`.
- [x] Déployé et vérifié (titre d'onglet, manifest.json, favicon).

## 2026-08-09 — Discussion business plan (monétisation)
- [x] Analyse des modèles "gratuits" (pub, subvention investisseurs façon Uber/Yango, freemium façon Todoist) — conclusion : la publicité est incompatible avec la confidentialité de Vigie, l'abonnement est le bon modèle (comme Clio/MyCase, les vrais concurrents indirects).
- [x] Pistes concrètes de répartition gratuit/payant inspirées de Todoist Pro : limite de dossiers actifs, fréquence de rappel (1x vs 2x/jour), chiffrement des notes, historique du bilan, pièces jointes, automatisation Gmail (payante, coût IA réel) — à trancher plus tard avec le chiffrage complet.
- [ ] **Ne pas oublier** (rappelé explicitement par Tobie) : faire une passe UX dédiée inspirée de Todoist/Sectograph (organisation des listes, simplicité de saisie, design épuré — sans copier leur design/marque), une fois les fonctionnalités cœur terminées (chiffrement, notifications).
- [ ] Achat d'un nom de domaine personnalisé (ex: vigie.app) — décidé explicitement comme l'une des toutes dernières choses à faire, pas urgent (l'app mobile ne montre pas cette URL).

## 2026-08-09 — Email de connexion (spam), bug du menu "Type de dossier", décision chiffrement
- [x] Personnalisation du modèle d'email Firebase Auth tentée (nom d'expéditeur "Vigie") pour réduire le risque de spam — **bloquée par un bug/restriction côté Firebase** ("Il n'est actuellement pas possible de mettre à jour les modèles d'e-mail de ce projet"), malgré adresse d'assistance et nom public correctement configurés. Reporté à plus tard (groupé avec l'achat du nom de domaine, qui permettra un vrai envoi authentifié SPF/DKIM). Non bloquant : la connexion par lien magique fonctionne, elle arrive juste dans les spams pour l'instant.
- [x] **Vrai bug corrigé** : le menu déroulant "Type de dossier" (écran Ajouter) restait toujours vide/non sélectionné quel que soit le choix. Cause : `NatureDossier` n'avait pas d'égalité (`==`) définie, donc chaque nouvelle liste reçue de Firestore créait des objets "différents" aux yeux de Dart même pour le même document, et le menu perdait la sélection. Corrigé en comparant les objets par leur `id`. Rebuild + redéploiement effectués sur `secretaire-aje.web.app`.
- [x] **Red Team + décision sur le chiffrement des notes privées** : voir [Notes/2026-08-09-redteam-chiffrement-notes.md](Notes/2026-08-09-redteam-chiffrement-notes.md). Proposition initiale (clé générée à sauvegarder par le père en dehors du téléphone) refusée par Tobie — trop de risque de perte définitive pour son profil. Alternative "chiffrement récupérable" proposée, mais **Tobie a tranché : on abandonne le chiffrement du champ `notePrivee`**. Les notes restent protégées par les règles Firestore (accès limité au compte), le PIN/biométrie, et les noms de code — mais seraient lisibles en clair en cas de fuite directe de la base Firestore. Risque assumé en connaissance de cause. **Sujet clos**, à ne rouvrir que si Tobie le demande explicitement.

## 2026-08-09 — Email affiché dans Profil + barre de progression par dossier
- [x] Profil : affichage de "Connecté en tant que ...@..." en haut de l'écran (`FirebaseAuth.instance.currentUser?.email`) — corrige le fait qu'on n'avait aucun moyen de vérifier avec quel compte on était connecté.
- [x] Red Team + décision : voir [Notes/2026-08-09-redteam-progression-dossier.md](Notes/2026-08-09-redteam-progression-dossier.md). Barre de progression par dossier (tâches faites / total, y compris les tâches "en attente" pas encore actives — un dossier n'est vraiment fini que si TOUT est traité), affichée sur l'écran de détail du dossier et sur chaque carte dossier de "À traiter". Calcul en direct, aucun champ stocké séparément.
- [x] Build + déploiement vérifiés (analyse statique propre, redéployé sur `secretaire-aje.web.app`).

## 2026-08-12 — Décision Gmail (boîte secondaire), passage à Blaze, notifications quotidiennes
- [x] Feu vert du père pour l'automatisation Gmail : il utilisera une **adresse email secondaire dédiée** (il y transfère lui-même ce qu'il veut faire traiter), pas sa boîte pro courante — réduit le risque à presque rien par construction. Voir [Notes/2026-08-12-decision-gmail-boite-secondaire.md](Notes/2026-08-12-decision-gmail-boite-secondaire.md). Pour les futurs utilisateurs payants : avertissement clair des risques au lieu d'imposer la même prudence.
- [x] Discussion IA gratuite (Gemini/Groq) vs payante : niveau gratuit de Gemini documenté par Google comme pouvant servir à l'entraînement de leurs modèles — risque de confidentialité incompatible avec des emails judiciaires réels, même filtrés sur un expéditeur de confiance. Décision : démarrer **sans IA** (recherche de date par motif + étape de confirmation avant toute création de dossier, donc aucune conséquence silencieuse d'une erreur), passer à l'IA payante plus tard une fois les revenus (abonnements) en place.
- [x] Passage au forfait **Blaze** effectué par Tobie. Carte bancaire utilisée technique (ancienne carte virtuelle Money Fusion détruite) acceptée par Google en vérification mais risque signalé : à remplacer par une carte valide non-prépayée avant toute mise en production commerciale (facturation réelle).
- [x] **Notifications quotidiennes construites et déployées** :
  - Flutter : `services/notification_service.dart` — demande la permission, récupère le token FCM, l'enregistre dans `/utilisateurs/{uid}`, écoute le rafraîchissement. Appelé au démarrage (`main.dart`). Permission Android 13+ (`POST_NOTIFICATIONS`) ajoutée au manifest.
  - Corrigé au passage : le label de l'app Android affichait encore l'ancien nom "Veillance" au lieu de "Vigie" — corrigé.
  - Cloud Function Python `functions/main.py` (`digest_quotidien`, planifiée 7h/19h, fuseau Africa/Porto-Novo) : pour chaque utilisateur, compte les tâches actives/en retard et envoie un digest ; si rien d'actif et aucun dossier ajouté depuis 7 jours, envoie le rappel "pense à ajouter tes dossiers" (point déjà identifié le 7 août). Nettoie automatiquement les tokens FCM invalides.
  - Runtime Python **3.14** (pas 3.12, faute de version disponible sur cette machine — supporté par Firebase, vérifié au déploiement).
  - Déployé avec succès (`digest_quotidien` actif sur `us-central1`) + politique de nettoyage des images de build configurée (évite une accumulation de coûts de stockage).
- [ ] **Reste à faire pour vérifier réellement** : un test de bout en bout nécessite un vrai téléphone Android (le web n'a pas la configuration complète des notifications push) — rejoint la tâche déjà en attente plus bas.

## 2026-08-14 — Test réel sur Android + notifications par dossier
- [x] **Environnement Android configuré sur cette machine** : SDK cmdline-tools + platform-tools + plateformes 34/35/36/37 + build-tools installés (`~/Android/Sdk`), `ANDROID_HOME` exporté dans `.bashrc`. `flutter_secure_storage` provisoirement pinné à `10.3.1` (la `11.0.0` exige le SDK 37, pas dispo proprement via `sdkmanager` sur cette machine — à revoir plus tard, sans urgence). Premier vrai APK construit et testé sur le téléphone du père (Redmi/MIUI).
- [x] **Bug de son/vibration corrigé** : Android fige les réglages (son/vibration) d'un canal de notification à sa toute première création — les préciser seulement côté serveur ne suffit pas. Le canal est maintenant créé explicitement côté app (`flutter_local_notifications`, nouvel id `rappels_vigie_v2`), avec desugaring Java 8 activé (`android/app/build.gradle.kts`) pour que le plugin compile.
- [x] **Vrai bug corrigé (Cloud Function)** : `_enregistrer_historique` et le nettoyage de token invalide utilisaient `.doc(uid)` (syntaxe Node.js) au lieu de `.document(uid)` (le SDK Python Firestore) → `AttributeError` silencieuse qui empêchait l'écriture de l'historique après un envoi pourtant réussi. Trouvé via une écriture de debug temporaire dans Firestore (`_debug/derniere_erreur`), faute de logs d'exécution fiables via `firebase functions:log` sur cette machine (n'affiche que les logs de déploiement, pas d'exécution — limite connue, pas un bug de l'app).
- [x] **Fausse piste identifiée** : plusieurs tests "sans notification" étaient dus à Tobie/moi inspectant le mauvais compte utilisateur dans Firestore (plusieurs comptes de test existent) — le token et l'envoi fonctionnaient depuis le début sur le bon compte.
- [x] **Réglage MIUI identifié comme bloquant réel** : au-delà des réglages Android standards (tous corrects), MIUI a son propre gestionnaire de permissions (démarrage automatique, restrictions batterie) qui peut bloquer silencieusement l'affichage — réglé côté téléphone par Tobie.
- [x] **Notifications repensées par dossier** (demande de Tobie) : au lieu d'un seul digest global, `functions/main.py` regroupe les tâches actives par dossier et envoie **une notification par dossier concerné**, avec `dossierId` dans le payload et dans l'historique Firestore (`/utilisateurs/{uid}/notifications`). L'écran "Notifications" de l'app (nouvel onglet, 5ᵉ destination) liste l'historique, filtrable par type de dossier, et **chaque entrée liée à un dossier est cliquable → navigue vers son détail**.
- [ ] **Déféré, pas oublié** : taper sur la notification **système** (pas juste dans l'onglet Notifications) pour naviguer directement au dossier — nécessite de gérer l'ouverture "à froid" de l'app depuis une notification (`getInitialMessage`/`onMessageOpenedApp`). Tobie a confirmé vouloir l'ajouter plus tard.
- [x] **Web et APK maintenus synchronisés** : Tobie a repéré que `secretaire-aje.web.app` n'avait pas été redéployé depuis plusieurs mises à jour de l'APK — corrigé (rebuild + déploiement web incluant toutes les fonctionnalités du jour).
- [x] **Chaîne complète confirmée de bout en bout** (téléphone déchargé en cours de route → bascule sur Waydroid, connecté en réseau via adb) : notification par dossier reçue, visible dans l'onglet Notifs, clic → navigation vers le bon dossier. Fonctionnalité "Notifications quotidiennes" considérée fonctionnelle et fermée pour cette phase. Horaire remis à la normale (7h/19h).

## 2026-08-15 — Automatisation Gmail : connexion OAuth construite
- [x] Décision actée : les deux méthodes d'extraction (motif de texte gratuit, IA payante) seront proposées, activables/désactivables librement par l'utilisateur — avec avertissement clair avant d'activer l'IA (le contenu complet des emails sort alors vers un service tiers). Le père est déjà au courant et a choisi la boîte secondaire précisément pour cette raison.
- [x] Configuration Google Cloud Console (guidée pas à pas avec Tobie) : API Gmail activée, écran de consentement OAuth en mode Test (utilisateurs test ajoutés : Tobie + un compte perso — adresse du père à ajouter dès qu'il la donne), champ d'application `gmail.readonly` déclaré, identifiant "Application Web" auto-créé par Firebase réutilisé (nouveau code secret généré, l'ancien reste actif mais inutilisé — à nettoyer plus tard).
- [x] Secrets stockés côté serveur uniquement (`firebase functions:secrets:set`, jamais dans le code) : `GOOGLE_OAUTH_CLIENT_SECRET` et `TOKEN_CHIFFREMENT_CLE` (clé de chiffrement du jeton Gmail, générée aléatoirement, jamais vue par un utilisateur — pas de risque "phrase oubliée" puisque personne ne la mémorise).
- [x] Flutter : `services/gmail_service.dart` (connexion via `google_sign_in` v7, obtention d'un code d'autorisation serveur — le jeton réel ne transite jamais côté téléphone), `screens/automatisation_screen.dart` (statut de connexion, sélecteur motif/IA avec avertissement, gestion des expéditeurs de confiance), lien ajouté depuis Profil.
- [x] Cloud Functions : `connecter_gmail` (échange le code contre un jeton, le chiffre avec `cryptography`/Fernet, le stocke) et `deconnecter_gmail`. Déployées avec accès aux secrets.
- [ ] **Pas encore testé de bout en bout** — à faire avec Tobie sur son téléphone (pas Waydroid, Google Sign-In risque d'avoir les mêmes limites de certification que les notifications push).
- [x] **Limite confirmée sur Web** : `authorizationClient.authorizeServer()` (obtention du code d'autorisation serveur) lève `UnimplementedError` sur Flutter Web — limite documentée du plugin `google_sign_in`, pas un bug de notre code. La connexion Gmail ne fonctionne donc pour l'instant que sur Android/iOS. Le formulaire (méthode d'extraction, expéditeurs de confiance) fonctionne bien sur web, seul le bouton de connexion est concerné.
- [x] **Connexion Gmail validée sur Android** : bug trouvé — `google-services.json` n'avait aucun identifiant OAuth Android enregistré (`oauth_client: []`, jamais fait depuis le début du projet). Corrigé : SHA-1 du keystore de debug de cette machine enregistré (`firebase apps:android:sha:create`), config retéléchargée. Connexion Google testée avec succès (bandeau "Google n'a pas validé cette application" normal, attendu en mode test).
- [x] **Vérification technique des expéditeurs de confiance ajoutée** (demande de Tobie, ma première solution — relecture seule — ne suffisait pas) : nouvelle fonction `verifier_domaine_email` (vérifie que le domaine a des serveurs mail configurés, via DNS/MX). Honnêteté actée : ça n'attrape que les fautes de frappe de domaine, impossible de garantir qu'une boîte précise existe (Gmail et les gros services bloquent cette sonde exprès) — combiné à la relecture déjà en place.
- [ ] **Suite prévue** : ajouter un vrai chemin web séparé (flux OAuth "Sign-In for server-side apps" de Google, différent du mécanisme mobile) — pas urgent, Android fonctionne.
- [ ] **Validation Google de l'app** (sortir du mode test / lever le bandeau "non validée") : nécessite une page de politique de confidentialité + conditions d'utilisation hébergées (lié à l'achat du nom de domaine), et pour `gmail.readonly` (catégorie "restreinte"), un audit de sécurité potentiellement payant. Pas urgent tant qu'on reste sous 100 utilisateurs test.
- [x] **Catalogue d'expéditeurs** (idée de Tobie, meilleure que la saisie manuelle) : nouvelle fonction `lister_expediteurs_recents` — lit les 50 derniers emails de la boîte connectée, extrait les expéditeurs uniques triés par fréquence. Nouvel écran de sélection (`_FeuilleChoixExpediteurs`, coche puis "Ajouter") — zéro risque de faute de frappe puisque choisi dans de vraies données, pas tapé. La saisie manuelle reste disponible en secours (avec vérification de domaine + relecture).
- [ ] Reste à construire : la fonction serveur qui lit réellement la boîte (filtrée par expéditeurs de confiance), l'extraction par motif (calibrée sur des formats de date larges, aucun exemple réel disponible), le chemin IA, et l'écran de confirmation avant création de dossier.

## 2026-08-15 (suite) — Distribution de l'app, malentendus résolus
- [x] **Cause racine trouvée pour "site introuvable"** signalé plusieurs fois par Tobie : confusion entre taper une adresse exacte (fonctionne, prouvé par `curl` + navigateur indépendant) et chercher "Vigie"/"secretaire-aje" sur Google (ne fonctionnera jamais tant que le site n'est pas indexé par Google — normal pour un site neuf, pas un bug). Confusion aggravée sur un appareil précis (tablette à écran fissuré) qui tapait dans la barre de recherche Google au lieu de la barre d'adresse du navigateur.
- [x] **Cause racine trouvée pour l'échec Gmail sur les appareils de la famille** : "Installer l'application" proposé par Chrome crée une icône qui **reste une version web** (PWA) — pas le vrai fichier `.apk` natif. D'où le même `UnimplementedError` que sur navigateur, y compris sur téléphone. Vérifié : le vrai `.apk` installé directement fonctionne bien (confirmé par Tobie sur la tablette).
- [x] **Distribution de l'APK simplifiée** : le fichier `.apk` est maintenant hébergé directement sur le site (`secretaire-aje.web.app/telecharger/Vigie.apk`), avec un bouton "Télécharger l'app Android" sur l'écran de connexion (visible seulement sur la version web). Fini l'envoi manuel par Tobie à chaque nouvel utilisateur.
- [ ] **Objectif à terme** : publier sur le Google Play Store (compte développeur ~25$, revue Google) pour une installation en un clic, sans manipulation de fichier `.apk`. Pas fait aujourd'hui, à programmer.
- [ ] Accident sans conséquence : app OAuth momentanément basculée en "Production" (non validée) par erreur, remise en mode "Test" — aucune vraie demande de validation n'a été envoyée à Google, rien à corriger.

## 2026-08-15 (suite) — Lecture des emails et propositions de dossiers
- [x] **Fonction planifiée `scanner_emails`** (même horaire que les rappels, 7h/19h) : pour chaque utilisateur avec boîte connectée + au moins un expéditeur de confiance + méthode "motif" active, cherche les emails récents (14 derniers jours) de ces expéditeurs, en extrait le texte (gère texte brut et HTML), cherche des dates par motif large (numérique et littéral en français), évite de retraiter un email déjà vu (`emailsTraites`). Chaque date trouvée devient une **proposition** en attente — jamais un dossier créé directement.
- [x] **Recherche de motif testée** sur un exemple synthétique : bonnes dates détectées, vieille référence de dossier correctement écartée (fenêtre de plausibilité -30/+400 jours).
- [x] **Écran "Propositions"** (Flutter) : liste des dates détectées (date, expéditeur, sujet de l'email), bouton "Créer le dossier" (ouvre un dialogue pour choisir le vrai nom de code + type + confirmer la date avant création réelle) ou "Ignorer" (double confirmation, suppression). Accessible depuis Automatisation (compteur visible) et depuis une pastille sur l'écran "À traiter".
- [x] Notification envoyée (et historisée) quand de nouvelles propositions sont détectées.
- [ ] **Pas encore testé avec de vrais emails** — dépend de la connexion Gmail du père, en attente qu'il soit disponible.
- [ ] Chemin IA : toujours non branché (pas de compte/clé API financé) — les comptes réglés sur "ia" sont ignorés par `scanner_emails` pour l'instant plutôt que de planter.

## 2026-08-17 — Connexion Gmail débloquée (père + Tobie), ajustements
- [x] **Connexion Gmail réussie pour le père et pour Tobie (2e compte)** — cause des deux blocages : adresses jamais ajoutées à la liste des utilisateurs test Google (`sindji.justice@gmail.com`, `tobieamadou1@gmail.com`), une règle Google documentée, pas un bug. Corrigé en les ajoutant.
- [x] **Point important découvert en lisant la doc Google officielle** (lien fourni par Tobie) : en mode test, la connexion Gmail d'un utilisateur ne dure qu'environ **7 jours** avant d'expirer (limite imposée par Google, pas nous) — reconnexion périodique à prévoir tant qu'on n'est pas validés.
- [x] Explication donnée à Tobie (pour son père) sur ce que fait vraiment l'automatisation : détecte et propose, ne remplit jamais tout seul — décision volontaire de sécurité, pas une limite technique. Reste vrai même avec l'IA (l'IA améliorerait la détection, pas la suppression de l'étape de confirmation).
- [x] Clarifié : le "500" du catalogue d'expéditeurs est un plafond, pas un objectif — le nombre réel affiché dépend du nombre de correspondants distincts dans la vraie boîte (80 est normal si c'est la réalité du compte testé).
- [x] Double confirmation ajoutée avant de retirer un expéditeur de confiance (suppression accidentelle possible en un clic, signalée par Tobie).
- [ ] **Toujours pas de proposition réelle observée** — en attente de confirmation qu'un expéditeur de confiance avec un email récent (< 14 jours) contenant une date a bien été configuré, pour déclencher un test immédiat.

## 2026-08-17 (suite) — Automatisation confirmée fonctionnelle + fréquence intelligente
- [x] **Premier vrai test de bout en bout réussi** : `scanner_emails` a réellement lu une boîte connectée, extrait de vraies dates d'emails réels, créé de vraies propositions visibles dans l'app. La chaîne complète (Gmail → extraction par motif → proposition → confirmation) est confirmée fonctionnelle, pas juste "construite en théorie".
- [x] Résultat inattendu du test : les propositions venaient d'un expéditeur `trade@alerts.ctrader.com` (alertes de trading, sans rapport) plutôt que de l'expéditeur de test prévu — probablement resté dans la liste de confiance depuis un test antérieur du catalogue. À vérifier/nettoyer par Tobie dans Firestore.
- [x] **Système de notification repensé** : au lieu de choisir entre "fréquent mais répétitif" et "rare mais silencieux", `digest_quotidien` ne prévient maintenant que si la situation d'un dossier a changé depuis le dernier envoi (nouveau suivi `/utilisateurs/{uid}/etatsNotifies/`). Permet de vérifier **toutes les 3h** (au lieu de 2x/jour) sans jamais répéter le même message. Le rappel d'alimentation générique garde un délai minimum de 20h entre deux envois (pas de "situation" à comparer pour lui). `scanner_emails` passe aussi à toutes les 3h (déjà naturellement silencieux, ne prévient que si de nouvelles propositions sont trouvées).

## 2026-08-17 (suite) — Clarification du besoin réel + ajustements
- [x] **Malentendu clarifié sur "automatisation"** : Tobie ne voulait pas remplacer la recherche de date, mais la **compléter** — quand la recherche de motif ne trouve aucune date dans un email d'un expéditeur favori (normal, elle ne "comprend" pas le sens comme une IA le ferait), l'app doit quand même prévenir plutôt que de laisser filer l'email en silence. Ajouté : `scanner_emails` compte maintenant aussi les emails "sans date trouvée" et envoie un rappel séparé ("ouvre ta boîte mail et ajoute la tâche toi-même").
- [x] Confirmé : le choix motif (père, confidentialité) vs IA (autres utilisateurs, une fois financé) reste un réglage par utilisateur, déjà en place, aucun changement nécessaire.
- [x] Renommage : "Expéditeur de confiance" → **"Expéditeur favori"** dans l'écran Automatisation (le nom technique interne `expediteursConfiance` reste inchangé en base, comme pour "nature"/"type" plus tôt dans le projet).
- [x] Texte du rappel d'alimentation corrigé : *"Rien à traiter pour l'instant, pense à ajouter des tâches aux dossiers de la semaine."* (suppression du mot "audiences", trop spécifique au métier de magistrat pour une app multi-métiers).
- [ ] Cause du "aucune proposition pour l'email de Marc" identifiée comme probable : le texte de l'email ne contient sans doute pas de date écrite explicitement (juste "demain"/"bientôt" par exemple) — à confirmer par Tobie.
- [ ] Nettoyage à faire par Tobie : retirer les expéditeurs favoris résiduels de test (`trade@alerts.ctrader.com`, `noreply@statuspage.io`, `googleone-updates-noreply@google.com`) qui ne sont pas de vrais contacts professionnels.

## 2026-08-17 (suite) — Notification "sans date" rendue précise
- [x] **Message trop vague corrigé** : le rappel envoyé quand un email d'un expéditeur favori ne contient aucune date détectable indiquait auparavant juste un compteur ("1 nouvel email..."), sans dire qui ni quand. Il précise maintenant **le nom exact de l'expéditeur, la date exacte d'envoi de l'email (format JJ/MM/AAAA, lue dans l'en-tête réel Gmail via `internalDate`) et le sujet** — un message séparé par email concerné, plus de regroupement en un seul compteur.
- [x] `_scanner_boite` renvoie maintenant la liste détaillée de ces emails (expéditeur, sujet, date d'envoi) au lieu d'un simple nombre ; nouvelle fonction `_date_envoi_email` pour lire la date réelle d'envoi Gmail.
- [x] Testé une première fois à vide (email déjà marqué "traité" lors d'un test antérieur au correctif — normal, pas un bug), puis confirmé fonctionnel avec un email réellement nouveau.
- [x] Horaire de vérification remis à sa valeur définitive **toutes les 3h** (`0 */3 * * *`) après la période de tests à horaire forcé.
- [x] Code vérifié (`import main` sans erreur) puis déployé (`firebase deploy --only functions`) — 6 fonctions mises à jour avec succès (`connecter_gmail`, `deconnecter_gmail`, `digest_quotidien`, `lister_expediteurs_recents`, `scanner_emails`, `verifier_domaine_email`).
- [ ] Nettoyage des 3 expéditeurs favoris résiduels de test toujours à confirmer par Tobie (`trade@alerts.ctrader.com`, `noreply@statuspage.io`, `googleone-updates-noreply@google.com`).

## 2026-08-17 (suite) — Catalogue d'expéditeurs : DEADLINE_EXCEEDED corrigé
- [x] **Bug signalé par Tobie (capture d'écran)** : ouverture de "Expéditeurs favoris" → catalogue → erreur `[firebase_functions/deadline-exceeded] DEADLINE_EXCEEDED`. Cause : `lister_expediteurs_recents` (côté serveur) a jusqu'à 300s pour scanner jusqu'à 500 emails, mais l'appel côté téléphone (`gmail_service.dart`) n'avait aucun délai précisé — le plugin Flutter coupe avant, avec une boîte mail chargée comme celle du père.
- [x] **Corrigé** : délai explicite de 300s ajouté côté client (`HttpsCallableOptions(timeout: Duration(seconds: 300))`) sur cet appel précis — aligné sur le serveur.
- [x] Bonne nouvelle constatée sur la même capture : **11 propositions détectées automatiquement** en conditions réelles — la chaîne complète (Gmail → extraction par motif → proposition) fonctionne bien pour de vrai.
- [x] Nouvel APK reconstruit et republié (web + téléchargement) avec ce correctif — `firebase deploy --only functions` (fait précédemment) puis `flutter build apk --release` + `flutter build web --release` + `firebase deploy --only hosting`, tous réussis.
- [ ] Tobie doit réinstaller l'APK à jour sur le téléphone de son père (et celui de sa mère si utilisé) pour bénéficier du correctif — l'ancien APK installé contient encore le bug.

## 2026-08-17 (suite) — Emails sans date dans Propositions + mise à jour intégrée
- [x] **Clarifié avec Tobie** (question posée directement) : les emails d'expéditeurs favoris sans date détectée doivent maintenant AUSSI créer une Proposition (sans date pré-remplie, à choisir soi-même dans l'écran de création) — en plus de la notification déjà précise. `_scanner_boite` (functions/main.py) modifié en ce sens. Le modèle Flutter `PropositionDossier` et l'écran `propositions_screen.dart` géraient déjà une date `null` ("Date inconnue" + sélecteur de date à l'ouverture) — aucun changement Flutter nécessaire pour cette partie.
- [x] **Mise à jour de l'app intégrée** (demandé par Tobie : "chiant de toujours avoir à aller sur le site") : bannière dédiée affichée au lancement de l'app (jamais mélangée aux vraies notifications), visible uniquement sur Android/iOS (la version web se met toujours à jour toute seule au rechargement). Compare le build local (pubspec.yaml, `package_info_plus`) à `/config/version` dans Firestore (lecture publique, écriture bloquée à tous — voir firestore.rules). Un clic sur "Télécharger" ouvre directement le nouvel APK.
- [x] `lib/utils/telechargement_stub.dart` (utilisé hors web) n'était qu'un no-op jusqu'ici — rendu fonctionnel via `url_launcher` (mode `externalApplication`, ouvre le navigateur système) pour permettre ce téléchargement direct sur Android.
- [x] **Publication de version protégée par secret** : cette machine n'a pas d'identifiants Google Cloud (ADC) pour écrire directement dans Firestore avec l'Admin SDK Python — ajout d'une fonction serveur `publier_version_admin` protégée par un secret dédié (`ADMIN_PUBLICATION_SECRET`, même modèle que les autres secrets du projet), avec vérification que l'URL de l'APK reste toujours sur notre propre domaine (`secretaire-aje.web.app`) même si le secret venait à fuiter — évite qu'une mise à jour malveillante puisse un jour être poussée aux utilisateurs.
- [x] Script `scripts/publier_version.py` créé pour appeler cette fonction après chaque nouvelle build (`ADMIN_PUBLICATION_SECRET=... python3 scripts/publier_version.py <version> <build>`).
- [x] Version 1.0.1 (build 2) publiée et vérifiée (lecture publique confirmée via `curl`) ; APK reconstruit avec toutes les fonctionnalités de cette session, republié (web + téléchargement).
- [ ] **Non testable depuis cette session** : le rendu réel de la bannière de mise à jour et le clic "Télécharger" sur un vrai téléphone Android n'ont pas pu être vérifiés directement (pas d'accès à l'appareil) — à confirmer par Tobie après avoir réinstallé le nouvel APK une première fois manuellement (ensuite les mises à jour suivantes pourront passer par la bannière).
- [ ] Pour publier une future version : régénérer/relire le secret `ADMIN_PUBLICATION_SECRET` (`firebase functions:secrets:access ADMIN_PUBLICATION_SECRET`, nécessite confirmation) avant de relancer le script.

## 2026-08-17 (suite) — Bannière de mise à jour testée et corrigée en conditions réelles
- [x] **Test réel réussi** : bannière "Nouvelle version disponible" bien visible en haut de l'écran principal (capture d'écran de Tobie) après publication d'une fausse version supérieure — le mécanisme de détection fonctionne.
- [x] **Bug trouvé par Tobie** : le bouton "Télécharger" ne faisait rien, aucune erreur visible. Cause réelle : il manquait la déclaration Android `<queries>` (obligatoire depuis Android 11/API 30 pour la "package visibility") autorisant l'app à ouvrir un navigateur externe — `url_launcher` échouait silencieusement sans elle. Corrigé dans `android/app/src/main/AndroidManifest.xml`.
- [x] Corrigé au passage : les erreurs de téléchargement futures s'afficheront désormais dans un message visible (SnackBar) au lieu de disparaître silencieusement.
- [x] **Double confirmation ajoutée** avant de fermer la bannière (demandé par Tobie, même principe que pour la suppression d'un expéditeur favori) — un clic accidentel sur la croix ne fait plus disparaître l'alerte sans confirmer deux fois.
- [x] Confirmé à Tobie : la bannière réapparaît de toute façon à chaque lancement complet de l'app tant que la version installée n'a pas rattrapé celle publiée — donc "récurrente jusqu'à mise à jour effective" comme demandé, même en cas de fermeture accidentelle.
- [x] Bug de compilation détecté et corrigé pendant le travail : incohérence de signature entre la version web (`void`, synchrone) et native (`Future<void>`, async) de `declencherTelechargementFichier` — la version web harmonisée en `Future<void>` pour que le code appelant (`await ...`) compile sur les deux plateformes.
- [x] Version réelle **1.0.2 (build 3)** buildée avec tous ces correctifs et republiée (web + APK), après avoir remis la vraie valeur à la place de la fausse version de test utilisée pour la vérification.
- [ ] Tobie doit réinstaller ce nouvel APK (1.0.2) une dernière fois manuellement sur le téléphone de son père — ensuite la bannière elle-même pourra servir pour les prochaines mises à jour, si le bouton "Télécharger" fonctionne bien cette fois (à confirmer).

## 2026-08-17 (suite) — Bouton "Télécharger" abandonné, pivot vers notification push
- [x] **Diagnostic approfondi du bouton cassé** : ajout d'une vérification du retour de `launchUrl` (il peut renvoyer `false` silencieusement sans lever d'erreur — c'était le cas ici, confirmé par un test réel : toujours aucun message, aucune notification de téléchargement Chrome, sur le téléphone du père). Manifeste Android vérifié directement dans l'APK compilé (`aapt2 dump xmltree`) : la déclaration `<queries>` nécessaire y est bien présente — donc pas un problème de build. Cause exacte non identifiée malgré plusieurs tentatives (probable restriction spécifique à Xiaomi/HyperOS sur ce téléphone précis, invérifiable sans accès physique/logcat).
- [x] **Décision prise avec Tobie (2026-08-17)** : abandon du bouton de téléchargement direct dans l'app — trop de temps passé sur un problème non réglable à distance. Retour à la méthode déjà fiable et prouvée (télécharger depuis le site).
- [x] **Bannière simplifiée** : garde l'alerte visuelle (fonctionne bien, testée) mais retire le bouton cassé, remplacé par un texte clair : "Va sur secretaire-aje.web.app pour la télécharger."
- [x] **Vraie notification push ajoutée** : `publier_version_admin` envoie maintenant une notification à tous les utilisateurs ayant un token FCM à chaque publication de version (en plus d'écrire `/config/version`), avec le numéro de version et le rappel du site — répond à la demande explicite de Tobie ("il faut que le site le notifie bien visuellement et aussi envoie une notification d'alerte").
- [x] Version réelle **1.0.4 (build 5)** publiée avec ce nouveau mécanisme — 3 utilisateurs notifiés (premier vrai envoi, à confirmer par Tobie que la notification est bien arrivée).
- [ ] Tobie doit réinstaller cette version depuis le site une dernière fois, et confirmer avoir bien reçu la notification push de publication.

## 2026-08-17 (suite) — Lien cliquable + version affichée sur le site
- [x] **Lien cliquable rétabli dans la bannière** (demandé par Tobie) : "secretaire-aje.web.app" est maintenant un vrai lien souligné, cliquable, qui tente d'ouvrir le site (pas directement l'APK cette fois) — avec le même filet de sécurité que précédemment (message d'erreur visible si ça échoue). Honnêteté à noter : peut échouer sur le même téléphone que le bouton précédent, cause jamais identifiée avec certitude — mais coût nul à tenter, et le texte explicite + la notification push restent le filet de secours garanti si le clic ne marche pas.
- [x] **Numéro de version affiché sur le site** (demandé par Tobie) : le bouton "Télécharger l'app Android" affiche maintenant "(v1.0.5)" — lu depuis `/config/version` (le même document déjà utilisé par la bannière), donc toujours synchronisé automatiquement avec la dernière publication.
- [x] Version réelle **1.0.5 (build 6)** buildée, déployée, publiée — 2 utilisateurs notifiés par push (1 de moins que la dernière fois, probablement un token FCM pas encore ré-enregistré après une réinstallation — pas inquiétant en soi, à surveiller si ça persiste).
- [ ] Tobie doit confirmer : (1) réception de la notification push, (2) si le lien cliquable dans la bannière fonctionne cette fois, (3) que le site affiche bien "(v1.0.5)" sur le bouton.

## 2026-08-17 (suite) — Clôture définitive du sujet "clic dans l'app"
- [x] **Décision finale actée avec Tobie** : abandon de toute tentative d'ouvrir un lien/navigateur depuis l'intérieur de l'app (bouton, texte cliquable) — cause du blocage sur le téléphone du père jamais identifiée avec certitude malgré plusieurs approches (vérification manifeste Android, vérification du retour de `launchUrl`, changement de cible URL). Retour au texte simple non cliquable dans la bannière, pour ne pas laisser croire à un lien fonctionnel qui ne l'est pas.
- [x] **Ce qui fonctionne et reste en place** : bannière visuelle (détection correcte, testée), **vraie notification push confirmée reçue** par Tobie à chaque publication, site web (toujours fiable pour le téléchargement manuel, prouvé à chaque test), numéro de version affiché sur le bouton du site.
- [x] Version finale de cette série : **1.0.6 (build 7)**, publiée et vérifiée.
- [x] Chemin de mise à jour définitif documenté : notification push alerte → utilisateur va sur `secretaire-aje.web.app` manuellement → bouton du site (fiable) télécharge → réinstallation manuelle. Pas d'automatisation du clic, assumé comme limite définitive plutôt que sujet ouvert.

## 2026-08-18 — Réorganisation : vérifier l'existant avant d'ajouter du neuf
- [x] **Malentendu sur "notification cliquable" enfin résolu** : le lien de connexion par email et le lien de la notification de mise à jour se disputaient le même domaine dans l'intent-filter Android (`android:host="secretaire-aje.web.app"` sans restriction de chemin) — la règle, trop large depuis le début, redirigeait TOUT vers l'app au lieu du navigateur. Corrigé en limitant l'intent-filter au chemin réel de connexion (`pathPrefix="/connexion"`), trouvé par élimination après un premier essai raté avec `/__/auth`. Explique enfin, avec certitude, tous les échecs de clic de cette session (bouton "Télécharger", lien banni, lien notification) — ce n'était PAS un bug MIUI/téléphone comme supposé, c'était notre propre configuration.
- [x] **Vrai bug trouvé et corrigé — biométrie inopérante depuis le début** : signalé par Tobie ("le verrou biométrique ne marche pas, c'est juste le code PIN"). Cause : `MainActivity.kt` utilisait `FlutterActivity` au lieu de `FlutterFragmentActivity`, requis par le plugin `local_auth` — sans ça, `authenticate()`/`canCheckBiometrics` échouent, masqué par un `try/catch` qui avalait l'erreur silencieusement. Corrigé.
- [x] **Recadrage explicite avec Tobie** : plusieurs affirmations ("notifications toutes les 3h fonctionnelles", "Gmail testé et fonctionnel", "bannière opérationnelle") reposaient sur UN SEUL test ponctuel ancien, pas une vérification continue — Tobie a eu raison de ne pas prendre ça pour argent comptant après plusieurs bugs découverts dans la même session. Décision actée : **vérifier l'existant avant d'ajouter des fonctionnalités neuves** (priorité choisie explicitement par Tobie face à 3 options).
- [x] Idée de Tobie notée pour plus tard : intégrer **Google Tasks** (au lieu de/en plus de Gmail) comme source de test plus fiable — données déjà structurées (titre + date), zéro besoin de regex/IA pour l'extraction. Question de conception posée, pas encore tranchée : passer par l'écran Propositions (comme Gmail) ou dossier générique automatique "Tâches Google" ?
- [x] Clarification IA : Tobie pensait à des offres gratuites (Gemini, Groq Cloud), pas un abonnement payant classique — mais rappel fait de la raison du 12 août (Gemini gratuit documenté par Google comme pouvant servir à l'entraînement, risque pour du contenu judiciaire). Politique de Groq sur ce point pas encore vérifiée — à faire avant de trancher, pas supposer.
- [x] Version **1.0.9 (build 10)** avec le correctif biométrique — build en cours, à publier et faire tester par Tobie.
- [ ] **Protocole de vérification sur plusieurs jours réels à faire avec Tobie** (pas plus de code, juste observer) :
  - Biométrie : tester le déverrouillage par empreinte/visage après réinstallation de la 1.0.9.
  - Notifications : observer sur 24-48h que les rappels arrivent bien ~toutes les 3h en cas de changement, sans répétition du même message.
  - Gmail : envoyer un nouvel email réel (jamais scanné) depuis un expéditeur favori, avec une vraie date, confirmer l'apparition d'une proposition dans le cycle suivant.
  - Bannière de mise à jour : confirmer qu'elle apparaît/disparaît correctement au fil des vrais lancements de l'app (pas seulement en test forcé).

## 2026-08-18 (suite) — Intégration Google Tasks
- [x] **Demande de Tobie** : tester l'automatisation avec de vraies données qu'il utilise déjà (Google Tasks), plutôt que d'attendre des emails réels. Décision actée : les tâches importées passent par l'écran **Propositions**, comme pour Gmail (même validation manuelle avant création d'un dossier).
- [x] `gmail_service.dart` : le même bouton "Connecter une boîte mail" demande maintenant aussi le scope `tasks.readonly` (une seule autorisation Google pour les deux).
- [x] Nouvelle fonction planifiée `scanner_taches_google` (functions/main.py, même horaire que les autres) : lit toutes les listes Google Tasks, importe chaque tâche non terminée **avec une échéance** comme Proposition (`expediteur: "Google Tasks"`, titre dans `sujetEmail`) — réutilise exactement le même schéma Firestore que les propositions Gmail, donc **aucun changement d'écran nécessaire** côté Flutter (propositions_screen.dart gérait déjà l'affichage générique). Les tâches sans échéance ne sont volontairement pas importées (beaucoup de gens utilisent Tasks comme simple liste sans date — les importer noierait les vraies échéances).
- [x] Suivi des tâches déjà importées via nouvelle sous-collection `tachesGoogleTraitees` (règle Firestore ajoutée, server-only comme `emailsTraites`).
- [x] Version **1.1.0 (build 11)** buildée, déployée, publiée.
- [ ] **Étape manuelle OBLIGATOIRE avant que ça fonctionne** : le scope `tasks.readonly` doit être ajouté à l'écran de consentement OAuth dans Google Cloud Console (même principe que `gmail.readonly` le 15 août) — sans ça, la nouvelle autorisation échouera. Détails donnés à Tobie en réponse.
- [ ] Une fois le scope ajouté côté Google Cloud : Tobie (et son père) doivent **se déconnecter puis se reconnecter** dans Automatisation pour que la nouvelle autorisation soit demandée (l'ancien jeton ne couvre que Gmail).

## 2026-08-18 (suite) — Intégration Google Calendar
- [x] **Demande de Tobie, juste après Tasks** : "on ne peut pas parler de Google Tasks sans Google Calendar". Même principe exact que Tasks : scope `calendar.readonly` ajouté à la même autorisation (un seul clic couvre maintenant Gmail + Tasks + Calendar), nouvelle fonction planifiée `scanner_calendrier_google` qui importe les événements des 30 prochains jours du calendrier principal comme Propositions (titre + date, `expediteur: "Google Calendar"`).
- [x] Décision assumée sans filtrage par pertinence (anniversaires/perso inclus) — cohérent avec le principe déjà en place : rien n'est décidé automatiquement, l'utilisateur ignore ce qui ne correspond pas à un dossier au moment de la validation dans Propositions.
- [x] Suivi via nouvelle sous-collection `evenementsGoogleTraites` (règle Firestore ajoutée, server-only).
- [x] Textes de l'écran Automatisation mis à jour ("Boîte mail" → "Compte Google", bouton "Connecter une boîte mail" → "Connecter un compte Google") pour refléter que la connexion couvre maintenant 3 services.
- [x] Guidage pas à pas donné à Tobie pour la config Google Cloud Console (Bibliothèque des API → activer Google Tasks API ; Écran de consentement → Accès aux données → ajouter `tasks.readonly`) — fait et confirmé par Tobie (capture d'écran).
- [x] Version **1.2.0 (build 12)** buildée, déployée, publiée.
- [ ] **Étape manuelle restante, PAS ENCORE FAITE par Tobie** : activer "Google Calendar API" dans la Bibliothèque des API (même écran que pour Tasks) + ajouter le scope `.../auth/calendar.readonly` à l'écran de consentement OAuth (Accès aux données) — sans ça, la nouvelle autorisation échouera comme pour Tasks avant activation.
- [ ] Une fois les 2 scopes (Tasks + Calendar) actifs côté Google Cloud : réinstaller la 1.2.0, puis **Automatisation → Déconnecter → reconnecter** pour que les 3 autorisations (Gmail + Tasks + Calendar) soient demandées ensemble.

## 2026-08-19/20 — Écran Agenda (calendrier visuel Google) + clé Groq
- [x] **Demande du père** (via Tobie) : voir son agenda Google directement dans Vigie, en vrai calendrier visuel — pas juste des Propositions silencieuses. Nouvel écran `AgendaScreen` : grille du mois (package `table_calendar`) avec les jours ayant des événements marqués, liste des événements du jour sélectionné en dessous. Accessible via une icône calendrier dans la barre du haut de "À traiter" — **pas de nouvel onglet**, décision explicite de Tobie pour ne pas surcharger la navigation.
- [x] Nouvelle fonction callable `lister_evenements_calendrier` (lecture en direct via l'API Calendar, distincte de `scanner_calendrier_google` qui importe en Propositions).
- [x] Locale française initialisée (`initializeDateFormatting('fr_FR')` dans `main.dart`) — nécessaire pour que `table_calendar` affiche les mois/jours en français, sinon plantage au premier rendu.
- [x] **Point de sécurité important corrigé avec Tobie** : en configurant l'écran de consentement OAuth pour Tasks/Calendar, Tobie avait accidentellement ajouté beaucoup plus de champs d'application que nécessaire, dont `https://mail.google.com/` (accès Gmail **total**, y compris suppression définitive d'emails) — signalé et retiré avant de continuer. Seuls `gmail.readonly`, `tasks.readonly`, `calendar.readonly` (+ `userinfo.email`) doivent rester.
- [x] **Clé Groq obtenue et stockée** (`GROQ_API_KEY`, secret Firebase Functions) — vérifié au préalable que Groq ne s'entraîne pas sur les données clients, même en gratuit (politique "no training by default", identique gratuit/payant, option Zero Data Retention disponible) — corrige le blocage de confidentialité qui avait fait écarter Gemini gratuit le 12 août. Prête à être utilisée pour la refonte du Bilan.
- [x] Version **1.3.0 (build 13)** buildée, déployée, publiée.
- [ ] **Toujours en attente côté Tobie** : réinstaller depuis le site (obligatoire, changement de code), reconnecter (Automatisation → Déconnecter → reconnecter) pour que les autorisations Tasks/Calendar nettoyées soient bien celles demandées, puis tester l'écran Agenda.
- [ ] **Chantier suivant, pas commencé** : refonte du Bilan — Tobie veut les 3 axes ensemble (résumé écrit automatique via IA Groq, recommandations/alertes intelligentes, plus de statistiques/graphiques). Prochaine session de travail.

## 2026-08-20 (suite) — Agenda confirmé fonctionnel + création de tâche depuis l'agenda
- [x] **Agenda testé et confirmé fonctionnel** par Tobie après reconnexion (Déconnecter → Reconnecter) — grille du mois avec marqueurs, liste du jour, vrais événements affichés (captures d'écran).
- [x] **Point d'architecture soulevé et pas encore tranché** : le père a remarqué que l'agenda affiché est celui du **compte Google secondaire** (décision confidentialité du 12 août), pas son vrai agenda personnel/pro utilisé au quotidien — comportement volontaire de l'architecture, pas un bug, mais qui déçoit l'usage réel voulu. Deux options possibles, décision à prendre avec Tobie : (a) connecter le vrai compte du père (mais expose alors aussi sa vraie boîte Gmail), ou (b) permettre deux connexions Google séparées (une pour Gmail confidentiel, une pour Calendar/Tasks sur le vrai compte) — plus de travail mais garde la séparation voulue. **Non résolu, à traiter.**
- [x] **Nouvelle fonctionnalité demandée et livrée** : depuis l'écran Agenda, taper sur un événement existant (ex: "La paye : argent de poche") ouvre le dialogue de création de dossier avec le titre et la date déjà pré-remplis ; un bouton flottant "Ajouter une tâche" permet aussi de créer une tâche pour le jour sélectionné même sans événement existant.
- [x] Refactoring au passage : le dialogue de création (nom + type + date) était dupliqué dans `propositions_screen.dart` — extrait en widget partagé `DialogueCreationDossier` (`lib/widgets/`), réutilisé par Propositions et Agenda.
- [x] Version **1.3.1 (build 14)** buildée, déployée, publiée.
- [ ] **Google Tasks toujours pas vérifié en conditions réelles** — la reconnexion vient d'avoir lieu, le prochain cycle (jusqu'à 3h) devrait faire apparaître des propositions si le compte connecté a de vraies tâches avec échéance. À confirmer par Tobie.
- [ ] Décision à prendre : architecture single-compte vs double-compte pour Calendar/Tasks (voir ci-dessus) — bloque une vraie utilisation satisfaisante de l'Agenda pour le père tant que ce n'est pas tranché.

## 2026-08-20 (suite) — Détails des tâches/événements importés + Red Team bloc-notes
- [x] **Bug signalé par le père** : en important un événement/tâche Google, seul le titre apparaissait dans Vigie, les détails (ex: le planning complet de "PENTALIGHTCODE — Session soir") étaient perdus.
- [x] **Red Team du bloc-notes fait avant de coder** (demandé explicitement par Tobie) : découverte qu'un champ `notePrivee` existe déjà sur chaque Dossier depuis le 9 août, mais n'a jamais été branché à un écran — mort dans le schéma. Verdict : un espace de notes a un vrai intérêt (capturer les détails Google + notes libres), mais avec un risque propre à Vigie — un champ de texte libre est l'endroit où on oublie le plus facilement la discipline des noms de code. Décision : nouveau champ `notesDetaillees` **par tâche** (pas par dossier, pour coller à l'origine des données importées), avec un rappel de confidentialité affiché juste à côté à chaque utilisation.
- [x] Implémenté de bout en bout : `Tache.notesDetaillees` (modèle + Firestore), rempli automatiquement depuis la description Google Calendar / les notes Google Tasks à l'import, visible et modifiable dans le détail du dossier, visible directement dans la liste de l'Agenda (plus besoin de cliquer pour voir).
- [x] Le champ `notePrivee` du Dossier (celui resté mort) n'a pas été touché — reste un sujet séparé, à trancher explicitement si Tobie veut aussi une note globale par dossier en plus de celle par tâche.
- [x] Version **1.4.0 (build 15)** buildée, déployée, publiée.
- [ ] **Chantier de fond identifié, pas commencé** : Tobie a demandé une refonte du positionnement des onglets/mise en valeur de l'app, une étude des apps concurrentes (Google Calendar/Tasks, apps de séries et de notes vues en capture), la mise en avant des fonctionnalités exclusives de Vigie, et un système de thèmes personnalisables. Trop gros pour être fait à la volée — nécessite une vraie session dédiée avec une proposition structurée avant de coder. Pistes déjà notées : carte de chaleur/série de constance pour le Bilan (inspiré d'une app vue en capture), couleurs par type de dossier dans l'Agenda (inspiré de Google Calendar).

## 2026-08-20 (suite) — Thèmes personnalisables + réorganisation des onglets
- [x] **Clarification importante avec Tobie** : "je préfère qu'on le fasse sérieusement dans une prochaine session" avait été mal compris comme un report — corrigé immédiatement : c'est fait dans la foulée, la même session, "sérieusement" ne voulait pas dire "plus tard".
- [x] **Thèmes** : nouveau `ThemeService` (préférence locale à l'appareil, `SharedPreferences`) — 6 couleurs d'accent au choix (Indigo, Bleu, Sarcelle, Vert, Violet, Orange) + mode Système/Clair/Sombre, réglable depuis Profil → Thème. S'applique à toute l'app y compris l'écran de connexion.
- [x] **Réorganisation des onglets validée avec Tobie avant d'exécuter** (question posée, "oui vas-y") : passage de 5 à **4 onglets** — À traiter, **Agenda** (élevé au rang d'onglet, plus juste une icône — répond à la demande du père), Bilan, Profil. "Ajouter" devient un bouton flottant sur "À traiter" (même principe que celui de l'Agenda). "Notifs" devient une icône cloche dans la barre du haut, au même endroit que Propositions.
- [x] Version **1.5.0 (build 16)** buildée, déployée, publiée.
- [x] **Écran "Pourquoi Vigie"** créé (`pourquoi_vigie_screen.dart`) : 5 points forts mis en avant clairement (relance jusqu'à confirmation, confidentialité par noms de code, automatisation multi-source unifiée en un seul écran de validation, vrai bilan navigable, verrou propre à l'app) — accès mis en évidence tout en haut de Profil (carte colorée, pas une simple ligne perdue dans la liste).
- [x] Version **1.6.0 (build 17)** buildée, déployée, publiée — **toute la liste de retours de Tobie du 20 août est traitée** (détails Google visibles, bloc-notes avec Red Team, thèmes, réorganisation des onglets, mise en avant des points forts).

## 2026-08-20 (suite) — Approfondissement du Red Team : bug récurrence, transparence, sources, bloc-notes structuré
- [x] **Vrai bug corrigé, trouvé en creusant les propres données de Tobie** : les événements Google Calendar récurrents (ex: "PENTALIGHTCODE — Session soir", chaque mercredi) généraient une nouvelle Proposition à chaque occurrence, indéfiniment. Corrigé en dédupliquant par `recurringEventId` (stable pour toute la série) au lieu de l'id de l'instance — seule la première occurrence rencontrée crée une proposition.
- [x] **Transparence de synchronisation** (inspirée d'une app comparée) : nouveau suivi `/utilisateurs/{uid}/etatSync/{gmail|tasks|calendar}` écrit à chaque exécution (succès ou échec, avec le message d'erreur) par les 3 fonctions planifiées. Affiché dans Automatisation : "Gmail : OK — il y a 2h" / "Erreur — ...". Aurait révélé immédiatement le bug de scopes manquants d'aujourd'hui, sans avoir à le deviner.
- [x] **Propositions codées par couleur et filtrables par source** (Gmail rouge / Google Tasks bleu / Google Calendar orange), inspiré des calendriers colorés de Google Agenda — transformé pour Vigie en codant par origine des propositions plutôt que par calendrier.
- [x] **Bloc-notes structuré "spécial Vigie"** (pas un champ libre générique) : 3 prompts courts — "Reste à vérifier", "En attente de", "Autre" — au lieu d'un champ vide, pour pousser vers un usage professionnel et réduire le risque d'y taper une vraie info sensible par réflexe. Stocké comme un seul texte formaté (`lib/utils/notes_structurees.dart`), donc aucune migration de schéma ; le texte importé de Google (non structuré) atterrit automatiquement dans "Autre" sans rien perdre.
- [x] Version **1.7.0 (build 18)** buildée, déployée, publiée.
- [ ] **Reste du Red Team approfondi, pas encore fait** : Bilan enrichi (carte de chaleur, camembert par type de dossier, "jour le plus chargé", indicateur de fiabilité façon "X jours sans retard"), escalade du ton des rappels selon la proximité de l'échéance, export anonymisé du Bilan (texte/PDF, safe par construction grâce aux noms de code). Puis seulement après : refaire l'écran "Pourquoi Vigie" avec du vrai contenu à montrer.

## 2026-08-20 (suite) — Bilan enrichi, escalade des rappels, export anonymisé
- [x] **Bilan enrichi** (`lib/widgets/statistiques_bilan.dart`, section "Statistiques" repliable pour ne pas surcharger la vue par défaut) : carte de chaleur des 12 dernières semaines (activité réelle, pas juste du texte), camembert par type de dossier sur la période, "jour le plus chargé", et un indicateur de fiabilité — **volontairement pas une série façon jeu** (mal placée pour un outil professionnel) mais "X jours sans tâche terminée en retard", calculé honnêtement à partir des données réellement disponibles (pas d'historique jour-par-jour inventé).
- [x] **Escalade du ton des rappels** : `digest_quotidien` ne répète plus le même message identique — le ton se durcit avec l'ancienneté du retard (texte neutre → ⚠️ dès 3 jours → 🔴 "à traiter en priorité" dès 8 jours) ou signale une échéance proche (≤ 2 jours) même sans retard. La détection "situation a changé" (qui évite le spam) tient maintenant compte du palier de ton, pas seulement des chiffres — un retard qui passe de 2 à 3 jours redéclenche une notification même si le total ne bouge pas.
- [x] **Export anonymisé du Bilan** : bouton de partage dans l'AppBar, génère un texte (noms de code, compteurs par dossier) via le partage natif du téléphone — sûr par construction, jamais de vraie information puisque les dossiers sont déjà en noms de code.
- [x] Version **1.8.0 (build 19)** buildée, déployée, publiée.
- [ ] **Dernière étape du Red Team approfondi** : refaire l'écran "Pourquoi Vigie" (créé trop tôt le 20 août, avant ce travail) avec du vrai contenu à montrer — carte de chaleur, escalade des rappels, export anonymisé, etc.

## 2026-08-20 (suite) — Bloc-notes sur l'écran principal + statistiques complétées
- [x] **Bug d'oubli trouvé par Tobie** : le bloc-notes structuré n'avait été branché qu'aux écrans secondaires (import Propositions/Agenda, édition dans un dossier) — l'écran "Ajouter" principal (le vrai point d'entrée quotidien, via le bouton flottant) n'avait rien. Corrigé : mêmes 3 champs ("Reste à vérifier"/"En attente de"/"Autre") + rappel de confidentialité, ajoutés à `ajouter_screen.dart`.
- [x] **Statistiques du Bilan complétées** : Tobie a repéré à raison que la carte de chaleur seule ne suffisait pas par rapport à l'app de référence — ajouté le vrai graphique en barres "tâches terminées par jour de la semaine" (`fl_chart` BarChart, pas juste le jour le plus chargé en texte) et le **taux d'achèvement** en pourcentage sur la période.
- [x] Version **1.8.1 (build 20)** buildée, déployée, publiée.
- [ ] **Dernière étape du Red Team approfondi** (inchangée) : refaire l'écran "Pourquoi Vigie" avec du vrai contenu, maintenant que le Bilan a vraiment de quoi être montré.

## 2026-08-20 (suite) — Journal de bord par dossier (le vrai bloc-notes demandé)
- [x] **Malentendu clarifié** : ce que Tobie voulait n'était pas "renommer détail en note" mais un vrai **journal** — des entrées datées qu'on ajoute au fil du temps pour documenter l'avancement étape par étape d'un dossier, jamais un champ qu'on écrase. Distinct des notes structurées par tâche (état ponctuel), qui restent en place.
- [x] Nouvelle collection `journalDossier` (top-level, même schéma que `taches` : `uid`, `dossierId`, `texte`, `creeLe`) — pas de "update" dans les règles Firestore, volontairement : un journal ne se corrige pas rétroactivement, on ajoute une entrée ou on supprime une erreur (double confirmation).
- [x] Nouvelle section "Journal de bord" dans l'écran de détail du dossier : champ + bouton pour ajouter une entrée, liste chronologique (plus récent en premier) en dessous.
- [x] **Index composite Firestore ajouté et déployé** (`firestore.indexes.json`) avant même le premier test — la requête (filtre par dossier + tri par date) en avait besoin, trouvé et corrigé de manière préventive plutôt que de laisser planter au premier usage réel.
- [x] Version **1.9.0 (build 21)** buildée, déployée, publiée (après une interruption de connexion sans conséquence, juste relancée).
- [ ] Dernière étape du Red Team approfondi (inchangée) : refaire "Pourquoi Vigie" avec du vrai contenu.

## 2026-08-20 (suite) — Journal de bord réparé + "Pourquoi Vigie" réécrit
- [x] **Vrai bug trouvé et corrigé** : "impossible de remplir le journal, ça s'auto-supprime" — la note s'enregistrait probablement bien, mais la lecture (StreamBuilder) n'affichait jamais l'erreur si l'index composite Firestore (tout juste déployé) n'était pas encore prêt : `snapshot.hasError` n'était jamais vérifié, donc un échec silencieux ressemblait à "aucune entrée". Corrigé : erreur affichée clairement si la lecture échoue, message d'erreur affiché aussi si l'ajout échoue (SnackBar) — plus aucun échec silencieux possible sur cet écran.
- [x] **Présentation améliorée** : section Journal maintenant dans une vraie carte dédiée (séparée visuellement de la liste des tâches), bouton "Ajouter au journal" pleine largeur au lieu d'une petite icône collée au champ de texte.
- [x] **Écran "Pourquoi Vigie" réécrit** avec du vrai contenu (comme prévu) : rappels qui s'adaptent, confidentialité par conception, automatisation multi-source avec transparence, bilan enrichi, journal de bord, export sûr par construction — plus les 5 points génériques du premier jet.
- [x] Version **1.9.2 (build 23)** buildée, déployée, publiée. **Le Red Team approfondi du 20 août est maintenant entièrement traité.**

## 2026-08-20 (suite) — Vrai bug du journal trouvé (capture d'écran) + refonte complète
- [x] **Cause exacte enfin confirmée par le message d'erreur montré par Tobie** : `[cloud_firestore/permission-denied]`. La requête de lecture du journal filtrait seulement par `dossierId`, pas par `uid` — Firestore refuse en bloc une requête de LISTE si elle ne prouve pas elle-même, par ses propres filtres, qu'elle ne peut renvoyer que des documents autorisés (contrairement à la lecture d'un seul document). Corrigé en ajoutant `.where('uid', isEqualTo: _uid)` à la requête (même principe déjà utilisé par `tachesDuDossier`), + index composite Firestore mis à jour en conséquence.
- [x] **Refonte de la conception, validée par Tobie avant construction** : chaque entrée est maintenant taguée au moment de l'écrire (Avancement / Blocage / Décision / Autre, choix rapide en boutons) et affichée en **vraie frise chronologique** (ligne verticale colorée reliant les entrées, icône/couleur par type) au lieu de cartes identiques génériques.
- [x] Version **1.10.0 (build 24) buildée, déployée, publiée**.
- [ ] **Leçon retenue pour la suite** : toujours demander le message d'erreur exact avant de corriger un bug rapporté comme "ça ne marche pas" — deviner a coûté plusieurs cycles inutiles ce soir, le message d'erreur affiché (ajouté justement pour cette raison) a permis de trouver la vraie cause en une fois.

## 2026-08-21 — Réorganisation de la hiérarchie du journal + publication 1.10.1
- [x] Sur demande de Tobie ("es que on peu revoir l'architecture et l'hierarchie du journal de bord"), regroupement des entrées du journal par jour civil : un en-tête de jour ("Aujourd'hui" / "Hier" / date formatée) affiché une seule fois, suivi des entrées de ce jour avec seulement l'heure (au lieu de répéter la date complète sur chaque ligne). Ajout d'un séparateur "Historique" entre le formulaire d'ajout et la liste des entrées passées, pour une séparation visuelle plus claire.
- [x] `flutter analyze` propre, version bumpée `1.10.0+24` → `1.10.1+25`, APK + web buildés, hosting déployé.
- [x] Version **1.10.1 (build 25) publiée** via `publier_version_admin` (2 utilisateurs notifiés) — publication interrompue une première fois par une coupure de connexion (secret temporaire perdu), relancée avec succès après régénération du secret.

## 2026-08-21 (suite) — Retour de test : édition du journal + désordre visuel
- [x] Tobie a testé 1.10.1 et remonté deux points, capture d'écran à l'appui : (1) impossible de modifier une entrée du journal, seulement la supprimer ; (2) visuellement, les types (Avancement/Blocage/Décision) sont mélangés dans la frise, "trop en désordre".
- [x] **Red Team fait avant de coder** : la modification libre contredit le principe "jamais écrasé" que Tobie avait lui-même posé le 20 août (règle Firestore sans `update`). Question posée à Tobie sur le compromis (fenêtre de correction courte vs. aucune modification vs. modification libre) — **Tobie choisit explicitement la modification libre à tout moment**, en connaissance du compromis expliqué.
- [x] Implémenté : règle Firestore `journalDossier` ouverte à `update` (uid/dossierId/creeLe protégés, seuls texte/type/`modifieLe` peuvent changer) ; méthode `modifierEntreeJournal` ; bouton crayon sur chaque entrée ouvrant un dialogue d'édition (mêmes chips de type + champ texte pré-rempli) ; marqueur "· modifié" affiché si l'entrée a été éditée.
- [x] Pour le désordre visuel : ajout de filtres par type (Tout/Avancement/Blocage/Décision/Autre, chips) au-dessus de la frise pour isoler un fil si besoin, tout en gardant l'ordre chronologique par défaut (voulu : le journal raconte une histoire dans l'ordre, pas triée par type) ; la ligne de connexion entre deux entrées est maintenant colorée selon le type au lieu d'un gris neutre uniforme, pour rester lisible d'un coup d'œil.
- [x] `flutter analyze` propre, version bumpée `1.10.1+25` → `1.10.2+26`, règles Firestore déployées, APK + web buildés et déployés, version **1.10.2 (build 26) publiée** (2 utilisateurs notifiés).
- [x] **Testé par Tobie sur téléphone réel, confirmé fonctionnel** : "ça marche bien, ça enregistre les modifications" — l'édition d'une entrée du journal (bouton crayon) enregistre correctement dans Firestore. Fonctionnalité journal (ajout, tag, frise chronologique groupée par jour, filtres par type, édition) considérée fermée pour cette phase.

## 2026-08-21 (suite) — Passe UX inspirée Todoist/Sectograph
- [x] **Point du 9 août enfin traité** ("organisation des listes, simplicité de saisie, design épuré — sans copier leur design/marque"). Analyse faite sur les deux écrans concernés (À traiter, Ajouter) avant de coder.
- [x] **Honnêteté actée avant de commencer** : le principe central de Sectograph (roue horaire 24h) ne s'applique pas à Vigie, l'app suit volontairement des échéances et non un planning horaire (décision actée le 2026-08-07) — seul l'esprit "épuré, un visuel qui parle plutôt qu'un mur de texte" a été repris, pas le concept lui-même.
- [x] Écran **Ajouter** : les 3 champs de notes (Reste à vérifier / En attente de / Autre) sont maintenant repliés par défaut dans un volet "Ajouter une note" (`ExpansionTile`) — le cas courant (juste nom + type + date) devient plus rapide à remplir, rien n'est perdu pour qui a besoin d'écrire une note.
- [x] Écran **Ajouter** : raccourcis de date rapides ("Dans 2 semaines" / "Dans 1 mois" / "Choisir une date") en plus du sélecteur complet, avec la date choisie affichée clairement en dessous.
- [x] Écran **À traiter** : le texte d'échéance verbeux ("Échéance dans 3 jours (21/08/2026)") remplacé par une pastille colorée compacte ("En retard"/"Aujourd'hui"/"Demain"/"Dans X j", rouge/orange/ambre/gris selon urgence) suivie de la date exacte en petit — plus scannable d'un coup d'œil.
- [x] Écran **À traiter** : le paragraphe d'explication permanent de la section "En attente" remplacé par une icône d'info cliquable (message affiché en SnackBar) — la liste elle-même reste toujours visible sans naviguer ailleurs (le vrai besoin identifié le 8 août : éviter les doublons), seul le mur de texte disparaît.
- [x] **Un point qui changeait un vrai comportement de sécurité, validé explicitement par Tobie avant d'être codé** : remplacement du bouton "C'est fait" par une case à cocher (plus épuré, façon Todoist) — mais Tobie a choisi de **garder la double confirmation** au clic plutôt que de passer à un marquage immédiat + annulation (option "immédiat + undo" proposée mais écartée). Compromis appliqué : `Checkbox` déclenchant toujours `demanderDoubleConfirmation()`.
- [x] `flutter analyze` propre, version bumpée `1.10.2+26` → `1.11.0+27` (bump mineur, plusieurs écrans changés), APK + web buildés et déployés, version **1.11.0 (build 27) publiée** (2 utilisateurs notifiés).
- [x] **Testé par Tobie sur téléphone réel, confirmé fonctionnel** : "ça marche bien, tout est fluide" — volet de notes replié, raccourcis de date, pastilles d'échéance et case à cocher tous validés en usage réel. Passe UX Todoist/Sectograph considérée fermée pour cette phase.

## 2026-08-21 (suite) — Deux connexions Google séparées (email confidentiel / agenda personnel)
- [x] **Ligne "Automatisation Gmail — pas encore commencée" de "Prochaine étape" corrigée** : elle était périmée depuis mi-août — l'automatisation (Gmail + Google Tasks + Google Calendar) est construite et testée en conditions réelles depuis longtemps (11 propositions détectées automatiquement le 17 août). Le vrai point encore ouvert, identifié en le vérifiant, était celui du 17 août resté "non résolu" : le père ne voyait que le calendrier/les tâches de son compte Google **secondaire** (confidentiel), pas son vrai compte du quotidien, parce qu'une seule connexion couvrait les 3 scopes ensemble sur le même compte.
- [x] **Décision de Tobie, après Red Team du compromis** : permettre **deux connexions Google totalement séparées et indépendantes**, plutôt que fusionner dans un seul compte. Chacune avec son propre bouton connecter/déconnecter dans Automatisation :
  - **"Boîte mail (confidentielle)"** — scope Gmail seul, le compte secondaire habituel.
  - **"Agenda & Tâches (compte personnel)"** — scopes Tasks + Calendar seuls, le vrai compte réel du père.
- [x] **Backend (`functions/main.py`)** : nouvelle sous-collection Firestore `/utilisateurs/{uid}/connexionsGoogle/{email|agenda}` (remplace l'ancien champ plat unique `gmailRefreshTokenChiffre`/`gmailEmailConnecte`) — `connecter_gmail`/`deconnecter_gmail` prennent maintenant un paramètre `type`, `_jeton_acces_gmail` renommée `_jeton_acces_google(uid, type)`. Les 3 fonctions planifiées (`scanner_emails`, `scanner_taches_google`, `scanner_calendrier_google`) et `lister_evenements_calendrier`/`lister_expediteurs_recents` utilisent chacune la bonne connexion.
- [x] **Sécurité renforcée au passage** : règle Firestore dédiée pour `connexionsGoogle` (lecture seule côté client, écriture bloquée — seule la fonction serveur avec le SDK Admin peut écrire) — plus strict que l'ancien champ plat qui autorisait techniquement l'écriture cliente sur le jeton chiffré, jamais exploité mais un vrai relâchement corrigé.
- [x] **Flutter** : `GmailService.connecter()`/`deconnecter()` prennent un `TypeConnexionGoogle` (`email`/`agenda`), chacun demandant uniquement ses propres scopes OAuth ; nouveau flux `connexionsGoogle()` (map `{type: emailConnecté}`). `automatisation_screen.dart` refondu en deux cartes séparées (`_CarteConnexion`) au lieu d'un seul bloc ; `agenda_screen.dart` vérifie spécifiquement la connexion "agenda" (pas "email") pour afficher le calendrier ; `profil_screen.dart` affiche un compte des connexions actives (0/1/2).
- [x] `flutter analyze` propre, `import main` Python propre, version bumpée `1.11.0+27` → `1.12.0+28`, règles Firestore + 7 fonctions redéployées, APK + web buildés et déployés, version **1.12.0 (build 28) publiée** (2 utilisateurs notifiés).
- [ ] **Points à vérifier par Tobie/le père sur téléphone réel** (pas testable depuis cette session — connexion OAuth interactive impossible sans navigateur réel) : (1) les deux comptes Google doivent déjà être ajoutés sur le téléphone (Réglages Android → Comptes) pour que le sélecteur Google propose vraiment un choix au moment de connecter "Agenda" ; (2) comme c'est un nouveau modèle de données, il faut reconnecter les deux connexions depuis Automatisation après la mise à jour — l'ancienne connexion unique devient orpheline (rien à nettoyer, juste inutilisée) ; (3) confirmer que l'Agenda affiche bien le VRAI calendrier du père une fois connecté sur son compte personnel.

## 2026-08-21 (suite) — Retour de test : bouton "C'est fait" rétabli + comptes Google repensés en liste générique
- [x] Tobie a testé 1.12.0 et signalé deux points : (1) la case à cocher pour "C'est fait" (tentée le même jour, inspirée de Todoist) n'est finalement pas intuitive — il revient sur sa propre décision, veut retrouver le bouton texte + double confirmation d'avant. (2) Les deux connexions Google nommées "email"/"agenda" posent un vrai problème d'usage, avec capture d'écran à l'appui : une fois un compte connecté, impossible de le déconnecter, aucune distinction visuelle claire des comptes connectés — et surtout, Tobie ne voulait pas de rôles prédéfinis du tout : "l'utilisateur est libre de leur donner un rôle, lui-même qui personnalise... on peut connecter jusqu'à 3 à 5 comptes et pas besoin de prédéfinir des rôles".
- [x] **Bouton "C'est fait" rétabli** dans `a_traiter_screen.dart` (`FilledButton` au lieu de `Checkbox`) — la double confirmation n'a jamais changé dans les deux versions.
- [x] **Comptes Google repensés en profondeur** — abandon du modèle à deux connexions nommées ("email"/"agenda", construit plus tôt le même jour) au profit d'une **liste générique de comptes** (jusqu'à quelques-uns), aucun rôle imposé par l'app :
  - **Backend (`functions/main.py`)** : `connexionsGoogle` devient une sous-collection à IDs auto-générés (au lieu de docs fixes "email"/"agenda") ; chaque connexion obtient systématiquement les 3 scopes (Gmail + Tasks + Calendar) — plus de distinction de scope par compte. `_jeton_acces_google` remplacée par `_connexions_actives(uid)` + `_echanger_refresh_token(...)`, appelée en boucle. Les 3 scanners planifiés (`scanner_emails`, `scanner_taches_google`, `scanner_calendrier_google`) parcourent maintenant TOUS les comptes connectés d'un utilisateur (une erreur sur un compte n'empêche plus de scanner les autres) ; `lister_expediteurs_recents` et `lister_evenements_calendrier` agrègent les résultats de tous les comptes en une seule liste fusionnée.
  - **Suivi "déjà traité" imbriqué par connexion** (`connexionsGoogle/{id}/emailsTraites`, `.../tachesGoogleTraitees`, `.../evenementsGoogleTraites`) au lieu d'un suivi global par utilisateur — évite toute collision d'identifiant entre deux comptes différents, maintenant que plusieurs comptes peuvent coexister.
  - **Libellé personnalisable** : champ `libelle` (vide par défaut, affiche l'adresse email tant qu'il n'est pas renseigné) — modifiable directement depuis l'app via une règle Firestore étroite (seul ce champ est autorisé en écriture cliente, `create`/`delete` restent serveur uniquement).
  - **Flutter** : nouveau modèle `ConnexionGoogle` (`lib/models/connexion_google.dart`), `GmailService.connecter()` sans paramètre (toujours les 3 scopes), `deconnecter(connexionId)`, `renommerConnexion(connexionId, libelle)`. `automatisation_screen.dart` refondu : liste dynamique (`_LigneConnexion`, une ligne par compte avec bouton renommer/déconnecter bien visibles) + un seul bouton "Connecter un [autre] compte" en bas — corrige directement le bug de déconnexion introuvable et le manque de distinction visuelle remontés par Tobie. `agenda_screen.dart` et `profil_screen.dart` mis à jour pour la nouvelle liste.
- [x] `flutter analyze` propre, `import main` Python propre, version bumpée `1.12.0+28` → `1.12.1+29`, règles Firestore + 7 fonctions redéployées, APK + web buildés et déployés, version **1.12.1 (build 29) publiée** (2 utilisateurs notifiés).
- [ ] Comme pour 1.12.0, reconnexion nécessaire depuis Automatisation (nouveau modèle de données, l'ancien devient orphelin) — à confirmer par Tobie/le père sur téléphone réel.

## Prochaine étape
- [ ] Retirer le debug temporaire `_debug/derniere_erreur` une fois la chaîne de notifications confirmée stable sur plusieurs jours réels (pas juste des tests manuels).
- [ ] Revenir sur `flutter_secure_storage` (repasser à la dernière version) une fois le SDK Android 37 proprement disponible.
- [ ] Carte bancaire Blaze à remplacer par une carte valide non-prépayée avant toute mise en production commerciale.
- [ ] Chemin IA (Groq) pour l'extraction Gmail — clé stockée côté serveur mais rien de branché.
- [ ] Achat du nom de domaine — toujours en fin de liste, pas urgent.

## 2026-08-21 (suite) — Bug trouvé par Tobie : adresse email vide tant qu'on ne renomme pas
- [x] **Vrai bug trouvé** (capture d'écran de Tobie) : une fois un compte Google connecté, la ligne dans "Comptes Google" restait vide (aucune adresse affichée) tant qu'on ne renommait pas soi-même la connexion. Cause : `connecter_gmail` allait chercher l'adresse email côté serveur via `/oauth2/v2/userinfo`, mais le jeton d'accès obtenu est volontairement limité aux scopes Gmail/Tasks/Calendar (aucun droit de lire le profil) — l'appel échouait silencieusement et renvoyait une chaîne vide, jamais d'erreur visible.
- [x] **Corrigé** : l'adresse email est maintenant envoyée par le téléphone (`compte.email`, déjà disponible via l'authentification Google de base, sans droit supplémentaire à demander) au lieu d'être redemandée à Google côté serveur. L'appel `/oauth2/v2/userinfo`, devenu inutile, a été retiré de `connecter_gmail`.
- [x] Filet de sécurité ajouté au passage : `ConnexionGoogle.affichage` n'est plus jamais vide même si le libellé ET l'adresse manquent (cas des connexions faites avant ce correctif) — affiche "Compte connecté (reconnecte-toi pour voir l'adresse)" plutôt qu'une ligne blanche.
- [x] `flutter analyze` propre, `import main` Python propre, version bumpée `1.12.1+29` → `1.12.2+30`, fonction `connecter_gmail` redéployée, APK + web buildés et déployés, version **1.12.2 (build 30) publiée** (2 utilisateurs notifiés).
- [x] **Testé par Tobie sur téléphone réel, confirmé fonctionnel** : "ça marche bien, l'adresse s'affiche correctement" — bug de l'adresse email vide fermé pour cette phase.

## 2026-08-22 — Liste complète du reste à faire + navigation depuis la notification système
- [x] **Liste exhaustive demandée par Tobie** ("donne-moi la liste complète de tout ce qu'il reste à faire") : relecture de tout PROGRESS.md, beaucoup de cases non cochées étaient en fait déjà résolues plus tard dans le fichier sans jamais être remises à jour (même schéma que la ligne "Automatisation Gmail" trouvée périmée le 21 août). Liste nettoyée et ordre logique proposé : (1) nettoyage des expéditeurs favoris résiduels de test (Tobie, pas de code), (2) navigation notification → dossier, (3) Bilan : résumé + recommandations IA (Groq), (4) chemin IA Gmail, (5) observation passive de stabilité puis retrait du debug temporaire, (6) nom de domaine → page CGU/confidentialité → validation Google → Play Store, (7) carte bancaire Blaze, (8) `flutter_secure_storage`/chemin web séparé. Tobie a validé : "ok on y va".
- [x] **Item 2 fait : navigation directe depuis la notification système vers le dossier concerné** — demandé le 2026-08-14, resté en attente jusqu'ici. `NotificationService` (Flutter) expose un `ValueNotifier<String?> dossierAOuvrir`, rempli par `getInitialMessage()` (app relancée à froid par le tap) et `onMessageOpenedApp` (app ramenée au premier plan). C'est `_EcranPrincipal` (main.dart) qui écoute et navigue réellement — **choix de sécurité important** : cet écran n'existe qu'une fois le verrou PIN/biométrique passé, donc un tap sur une notification ne peut jamais court-circuiter le verrou d'une app pensée pour rester confidentielle. Aucun changement backend nécessaire (le `dossierId` était déjà dans le payload de chaque notification depuis le 14 août).
- [x] `flutter analyze` propre, version bumpée `1.12.2+30` → `1.13.0+31`, APK + web buildés et déployés, version **1.13.0 (build 31) publiée** (2 utilisateurs notifiés).
- [ ] À confirmer par Tobie sur téléphone réel : taper sur une notification de rappel ouvre bien directement le bon dossier, y compris app fermée (cold start) et app en arrière-plan.

## 2026-08-22 (suite) — Navigation notification cassée en démarrage à froid, correction tentée
- [x] Tobie a testé 1.13.0 (app complètement fermée, tap sur notification) : "reste bloqué sur À traiter" — pas de navigation vers le dossier, aucune erreur visible.
- [x] **Hypothèse de cause, trouvée par relecture du code** (leçon du 20 août appliquée : détail exact demandé avant de corriger) : dans `NotificationService.demarrer()`, la lecture de `getInitialMessage()` était placée APRÈS plusieurs étapes réseau (création du canal, permission, jeton FCM, écriture Firestore) dans le même bloc try/catch. Si une de ces étapes échoue au tout premier lancement à froid (assez courant : Play Services ou réseau pas encore prêts), l'exception est avalée silencieusement et `getInitialMessage()` n'est jamais atteint.
- [x] **Corrigé** : la lecture de `getInitialMessage()`/`onMessageOpenedApp` est maintenant isolée dans son propre bloc try/catch, exécuté en premier, indépendant de la suite (permission/jeton/Firestore).
- [x] `flutter analyze` propre, version bumpée `1.13.0+31` → `1.13.1+32`, APK + web buildés et déployés, version **1.13.1 (build 32) publiée** (2 utilisateurs notifiés).
- [ ] **Honnêteté actée** : correctif basé sur une relecture de code, pas confirmé par les logs réels du téléphone (aucun accès logcat depuis cette session) — à tester par Tobie. Si toujours cassé, prochaine étape : ajouter un indicateur de debug visible (même principe que `_debug/derniere_erreur` pour le bug Cloud Function du 14 août) pour voir précisément où ça coince.

## 2026-08-22 (suite) — Bouton "Créer" bloqué grisé sur événement Agenda pré-rempli
- [x] Tobie a repéré un autre bug en testant la navigation notification, avec capture d'écran : dans le dialogue "Ajouter à un dossier" (utilisé depuis Propositions et l'Agenda), ouvert entièrement pré-rempli en tapant sur un événement Google Calendar, le bouton "Créer" restait grisé/inactif alors que tous les champs étaient déjà remplis — impossible de valider sans d'abord toucher un champ, même sans rien changer dedans.
- [x] **Cause confirmée par lecture du code** (`dialogue_creation_dossier.dart`) : la nature par défaut (`_nature ??= natures.first`) était affectée directement dans le callback du `StreamBuilder`, sans passer par `setState()`. Le bouton "Créer" (conditionné à `_nature == null`) ne se réévaluait donc jamais après cette affectation silencieuse. Ça ne se voyait pas d'habitude parce que toucher n'importe quel autre champ (nom, date...) déclenchait un `setState()` qui, par ricochet, redessinait aussi le bouton avec la valeur déjà affectée — le bug ne se manifestait que si le dialogue restait totalement intact, exactement le cas d'un événement Agenda déjà complet.
- [x] **Corrigé** : l'affectation de la nature par défaut passe maintenant par un `setState()` différé (`WidgetsBinding.instance.addPostFrameCallback`), pour ne pas déclencher setState() en pleine construction.
- [x] `flutter analyze` propre, version bumpée `1.13.1+32` → `1.13.2+33`, APK + web buildés et déployés, version **1.13.2 (build 33) publiée** (2 utilisateurs notifiés). Confiance élevée sur ce correctif : le mécanisme trouvé en lisant le code correspond exactement à la description détaillée de Tobie.
- [ ] À confirmer par Tobie : le bouton "Créer" est bien actif dès l'ouverture d'un dialogue déjà pré-rempli, sans avoir à toucher un champ.

## 2026-08-22 (suite) — Tap sur notification n'ouvre même pas l'app : demande d'exemption batterie
- [x] Tobie a testé la navigation notification (1.13.1) sur un téléphone **Tecno (HiOS)**, pas celui de son père (Redmi/MIUI) : "ça ne me conduit nulle part, la notif disparaît juste" — précisé ensuite : l'app ne s'ouvre même pas du tout, il reste sur l'écran d'accueil du téléphone. Un problème plus profond que la navigation interne déjà corrigée (1.13.1) : si l'app ne se lance jamais, aucun code Dart ne peut agir.
- [x] **Remarque juste de Tobie, prise en compte** : "es que les créateurs de Google Task... vont voir physiquement les utilisateurs ou leur envoient un message..." — Vigie doit demander lui-même toutes les autorisations dont il a besoin via de vraies fenêtres système, comme il le fait déjà pour les notifications (acceptée sans problème par Tobie à l'ouverture), plutôt que de compter sur l'utilisateur pour aller fouiller dans les réglages.
- [x] **Distinction importante expliquée à Tobie** : il existe une vraie autorisation Android officielle avec fenêtre système (exemption de mise en veille forcée / optimisation batterie) — mais les réglages "démarrage automatique" propres à certaines marques (MIUI, Tecno/HiOS) n'ont AUCUNE fenêtre d'autorisation standard, aucune app (pas même Google Tasks) ne peut la faire apparaître — seule option : réglages à la main. Honnêteté actée sur cette limite.
- [x] **Implémenté** : dépendance `permission_handler` ajoutée, permission `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` déclarée dans `AndroidManifest.xml`, `NotificationService.demarrer()` demande maintenant l'exemption de mise en veille forcée via la vraie fenêtre système Android, juste après l'autorisation de notifications. Ce même type de restriction (MIUI) avait déjà bloqué la réception des notifications le 14 août sur le téléphone du père — plausible que ce soit la même famille de blocage ici, côté lancement au tap cette fois.
- [x] `flutter analyze` propre, build APK+web propres avec cette nouvelle dépendance native (surveillé attentivement, premier build avec ce plugin), version bumpée `1.13.2+33` → `1.14.0+34`, APK + web déployés, version **1.14.0 (build 34) publiée** (2 utilisateurs notifiés).
- [ ] À confirmer par Tobie : la fenêtre système d'exemption batterie apparaît bien à l'ouverture de l'app, et le tap sur une notification ouvre enfin l'app. Si toujours bloqué après acceptation de cette exemption, le blocage vient probablement du réglage "démarrage automatique" propre à HiOS/Tecno — à vérifier manuellement dans les réglages du téléphone (aucune solution côté code possible pour celui-là).

## 2026-08-22 (suite) — Point 3 de la liste : résumé + recommandations IA du Bilan (Groq)
- [x] Tobie a dit "je vais tester ça après, on continue" (navigation notification en attente de test) — reprise de la liste, point suivant : résumé écrit automatique + recommandations intelligentes du Bilan, demandé le 18 août dans le cadre des "3 axes" de la refonte du Bilan. Les statistiques/graphiques avaient déjà été faits (2026-08-20), il ne restait que la partie IA.
- [x] **Backend** : nouvelle fonction `generer_resume_bilan` (secret `GROQ_API_KEY`, déjà stocké depuis le 12 août — vérifié à l'époque que Groq n'entraîne pas sur les données par défaut, même en gratuit), modèle `llama-3.3-70b-versatile`. Envoie exactement le même texte anonymisé déjà utilisé pour le partage du Bilan (noms de code, jamais de vraies informations) à Groq, avec une consigne système en français demandant un résumé factuel court + 2-3 recommandations concrètes.
- [x] **Réutilisation plutôt que duplication** : la construction du texte anonymisé (`_texteBilan`) a été extraite de `_partager` en une méthode partagée, utilisée à la fois par le partage existant et le nouveau résumé IA — garantit qu'aucune donnée de plus ne part vers Groq que ce que l'utilisateur partage déjà volontairement ailleurs dans l'app.
- [x] **Désactivé par défaut, avertissement avant activation** — même principe que le chemin IA pour l'extraction Gmail (décision du 2026-08-15) : nouveau champ `bilanIaActif` (faux par défaut), nouvelle section `_SectionResumeIa` dans l'écran Bilan avec bouton "Activer" (fenêtre d'avertissement) puis "Générer"/"Régénérer". Le résumé affiché s'efface automatiquement si on change de période, pour ne jamais laisser un résumé obsolète sous des chiffres différents.
- [x] `flutter analyze` propre, `import main` Python propre, nouvelle fonction créée avec succès (accès au secret Groq accordé automatiquement au déploiement), version bumpée `1.14.0+34` → `1.15.0+35`, APK + web buildés et déployés, version **1.15.0 (build 35) publiée** (2 utilisateurs notifiés).
- [ ] À tester par Tobie : activer le résumé IA dans le Bilan, générer un résumé, vérifier que le contenu est pertinent et que le texte reste bien anonyme.

## 2026-08-22 (suite) — Questions légitimes de Tobie sur Groq, modèle corrigé
- [x] Tobie a posé de bonnes questions avant de faire confiance à la fonctionnalité : "tu l'as bien entraîné ?", "que doit-il faire ?", "es-tu sûr qu'on peut se fier à Groq ?", "as-tu choisi le meilleur modèle ?". Réponses données : aucun entraînement (juste des instructions envoyées à chaque appel à un modèle déjà existant), tâche limitée à lire un texte anonymisé et renvoyer du texte (aucune action possible), risque contenu par construction (lecture seule, opt-in, données déjà minimisées).
- [x] **Sur le "meilleur modèle" : vérifié en direct plutôt que suppose de mémoire** (connaissances figées à janvier 2026, secteur qui bouge vite) — recherche web + lecture de la documentation officielle Groq. Résultat : `llama-3.3-70b-versatile` (choix initial) n'est PAS déprécié mais n'est pas non plus leur meilleur modèle — `openai/gpt-oss-120b` est désigné explicitement par Groq comme leur modèle "flagship" qualité/raisonnement. Corrigé.
- [x] Changement backend uniquement (`_MODELE_GROQ`), aucun impact Flutter — fonction `generer_resume_bilan` redéployée avec succès, pas de nouvelle version d'app nécessaire.

## 2026-08-22 (suite) — "À traiter" : tâche coincée dans "En attente" (staleness)
- [x] Tobie a signalé un problème dans l'espace "En attente" de "À traiter" (délai de rappel 14 jours) sans réussir à l'expliquer précisément, et a demandé explicitement de vérifier le code et la logique directement plutôt que de deviner à partir de sa description.
- [x] **Bug structurel réel trouvé par relecture du code** (`a_traiter_screen.dart`, `tache.dart`) : la répartition "actif" / "en attente" compare `datePremierRappel` à `DateTime.now()`, mais ce calcul ne se refaisait que quand Firestore envoyait une mise à jour (écriture d'un document) — jamais juste parce que le temps passe. Une tâche pouvait donc rester affichée dans "En attente" **après** que sa date de rappel soit réellement passée, tant qu'aucune autre modification n'avait lieu ailleurs dans l'app pour forcer un rafraîchissement de l'écran.
- [x] **Corrigé** : `ATraiterScreen` transformé en `StatefulWidget` avec un `Timer.periodic` toutes les heures qui force un nouveau rendu (aucun coût réseau, juste un recalcul local avec l'heure actuelle).
- [x] **Honnêteté actée** : ce correctif a été trouvé par relecture du code, pas à partir d'un symptôme précis rapporté par Tobie (qui a dit avoir du mal à l'expliquer) — signalé qu'il peut ne pas correspondre exactement à ce qu'il observait, à confirmer après test ou décrire/capturer plus précisément si le problème persiste.
- [x] `flutter analyze` propre, version bumpée `1.15.0+35` → `1.15.1+36`, APK + web buildés et déployés, version **1.15.1 (build 36) publiée** (2 utilisateurs notifiés).

## 2026-08-22 (suite) — "En attente" : vrai bug trouvé, délai de rappel figé à la création
- [x] Le correctif précédent (Timer.periodic) était réel mais ne réglait pas le vrai problème : Tobie a testé et dit "non, je ne suis toujours pas satisfait", avec deux captures d'écran et une question précise (quels dossiers sont dans la marge du délai de rappel ?).
- [x] **Vrai bug trouvé par recoupement des captures d'écran** : certains dossiers ("omega", "PENTALIGHTCODE", "projet", "COMPTE BANCAIRE") utilisaient un délai effectif de 7 jours, d'autres ("BCE", "CPI USA") le délai actuel de 14 jours — tous mélangés dans "En attente" sans aucun moyen de les distinguer visuellement. Cause : `datePremierRappel` est calculée UNE SEULE FOIS à la création de la tâche, avec le délai actif à ce moment-là (`UtilisateurService.delaiRappelJours()`), puis stockée telle quelle pour toujours. Changer le réglage `delaiRappelJours` plus tard (7 → 14 jours, confirmé par Tobie) n'avait jamais de rétroactivité sur les tâches déjà créées.
- [x] **Directive explicite de Tobie** : pas seulement un bouton de recalcul manuel — "le changement du délai doit entraîner le recalcul automatique... il est tout à fait normal de changer le délai autant de fois qu'on veut, il ne devrait normalement y avoir aucun problème."
- [x] **Corrigé** : `UtilisateurService.definirDelaiRappelJours()` recalcule maintenant automatiquement, à chaque changement de réglage, la `datePremierRappel` de toutes les tâches en cours (`statut == aFaire`, hors tâches "immédiat" qui ne dépendent pas du délai) avec la nouvelle valeur — même principe déjà utilisé par `FirestoreService.modifierDossier()` pour la modification de date d'un dossier. Aucun bouton manuel séparé ajouté : redondant, le déclenchement automatique couvre déjà le cas.
- [x] `flutter analyze` propre, version bumpée `1.15.1+36` → `1.15.2+37`, APK + web buildés et déployés, version **1.15.2 (build 37) publiée** (2 utilisateurs notifiés).
- [x] Tobie a testé (capture d'écran, délai réglé à 7 jours) : le recalcul fonctionnait bien pour "omega" (correctement passé à "dans 2 jours"), mais la tâche "Courrier" du dossier "Anniversaire de Océane Ashley" restait active alors qu'elle était à 14 jours de l'échéance — persistance du problème signalée : "pourquoi tu n'arrives pas à régler le problème ??"
- [x] **Cause identifiée, PAS un raté du correctif précédent** : "Courrier" est un type de tâche préconfiguré depuis bien avant cette session (`profession.dart`), marqué `immediat: true` — conçu pour toujours apparaître tout de suite dès sa création, en ignorant volontairement le délai de rappel. Mécanisme différent, indépendant du bug corrigé en 1.15.2. Vérifié par lecture directe du code (`immediat: true` explicitement documenté comme "règle Courrier").
- [x] **Décision de Tobie, après explication** : supprimer complètement cette exception — "je veux qu'elles respectent aussi le délai comme les autres". Toutes les natures de tâche (Courrier, Lecture, Scan, et toutes les autres) doivent désormais suivre le même délai de rappel, sans exception.
- [x] **Concept "nature immédiate" retiré entièrement** (pas juste neutralisé) : champ `immediat` supprimé de `NatureDossier`, champ `natureImmediat` supprimé de `Tache`, `Tache.calculerDatePremierRappel()` simplifié (toujours `dateDeclenchante - délai`, plus de branche spéciale), toggle "Suivi immédiat (comme Courrier)" retiré de l'écran de gestion des types de dossier, tous les sites d'appel (création de dossier, ajout de tâche, modification de tâche, ajout groupé) mis à jour. Les anciens documents Firestore gardent leurs champs `immediat`/`natureImmediat` orphelins (aucun code ne les lit plus) — pas de script de migration nécessaire vu l'échelle de l'app.
- [x] Le recalcul automatique du 1.15.2 (`UtilisateurService.definirDelaiRappelJours`) s'applique maintenant à TOUTES les tâches en cours, sans exception — donc rouvrir Profil → Délai de rappel corrige aussi les anciennes tâches "Courrier"/"Lecture"/"Scan" comme les autres.
- [x] `flutter analyze` propre, version bumpée `1.15.2+37` → `1.15.3+38`, APK + web buildés et déployés, version **1.15.3 (build 38) publiée** (2 utilisateurs notifiés).
- [x] **Testé par Tobie sur téléphone réel, confirmé fonctionnel** : "ça marche, 'Courrier' est bien passé en 'En attente'" — l'espace "À traiter"/"En attente" affiche maintenant un délai cohérent pour tous les types de tâche, sans exception. Sujet fermé pour cette phase (staleness Timer.periodic + recalcul auto du délai + suppression du mécanisme "nature immédiate").

## 2026-08-22 (suite) — Point 4 de la liste : chemin IA Gmail branché
- [x] Reprise de la liste après le sujet "À traiter" clos : point 4, le chemin IA pour l'extraction des dates dans les emails (Groq), noté "clé stockée côté serveur mais rien de branché" depuis le 2026-08-15.
- [x] **Découverte en explorant le code avant d'implémenter** : le front était déjà entièrement prêt (`automatisation_screen.dart`) — le choix "Recherche de motif" vs "Intelligence artificielle" avec la fenêtre d'avertissement explicite ("le contenu complet de chaque email... est envoyé à un service tiers... pour des emails judiciaires réels, c'est une exposition à prendre au sérieux") existait déjà. Seul le côté serveur (`scanner_emails`) ignorait le réglage `methodeExtraction` et restait bloqué sur "motif".
- [x] **Différence de confidentialité actée avant d'implémenter** (règle "Sécurité non négociable") : contrairement au résumé IA du Bilan (texte anonymisé par noms de code), le chemin IA Gmail envoie le contenu RÉEL de l'email (objet + corps, pas anonymisé) à Groq — c'est exactement ce que la fenêtre d'avertissement déjà existante annonce, donc cohérent avec ce qui est promis, mais à ne jamais confondre avec le principe du Bilan.
- [x] **Implémenté** : nouvelle fonction `_extraire_avec_ia(sujet, texte)` — appelle Groq (mode JSON strict) pour extraire `dates` (liste, jamais devinées), `nomSuggere` (titre court) et `resume` (1-2 phrases), avec le même garde-fou anti-date-farfelue que la recherche de motif (entre -30 et +400 jours). En cas d'erreur Groq (indisponible, JSON invalide), renvoie un résultat vide plutôt que de faire échouer le scan — l'email retombe dans le chemin "sans date reconnue" déjà existant, jamais silencieux.
- [x] `_scanner_boite` prend maintenant un paramètre `methode` ("motif" ou "ia") et bascule entre les deux chemins d'extraction ; `scanner_emails` lit `methodeExtraction` de l'utilisateur et route en conséquence (au lieu d'ignorer purement et simplement "ia" comme avant) ; secret `GROQ_API_KEY` ajouté aux secrets de la fonction planifiée.
- [x] Le champ `details` des propositions (déjà supporté côté modèle Flutter, jusqu'ici vide pour les propositions Gmail) est maintenant rempli par le résumé IA quand la méthode "ia" est active — visible directement dans l'écran Propositions.
- [x] **Sécurité vérifiée avant déploiement** : `nomSuggere` (IA ou motif) n'est jamais pré-rempli automatiquement comme nom de code du dossier créé — `propositions_screen.dart` ne transmet que la date et les notes au dialogue de création, jamais le nom suggéré. L'utilisateur choisit toujours lui-même le nom de code, le principe d'anonymisation des dossiers reste intact même avec l'IA activée.
- [x] `import main` (Python) propre, `py_compile` propre, changement backend uniquement (aucun impact Flutter, pas de nouvelle version d'app) — fonction `scanner_emails` redéployée avec succès.
- [ ] À tester par Tobie (ou son père) : activer "Intelligence artificielle" dans Profil → Automatisation, vérifier qu'un email d'un expéditeur favori produit une proposition avec un titre et un résumé plus utiles qu'avec la recherche de motif.

## Outils installés
- `faster-whisper` (pip, transcription audio locale FR).
- Flutter 3.44.8 (snap), Java 17 (openjdk-17-jdk), Firebase CLI, FlutterFire CLI.
- Android SDK (`~/Android/Sdk`) : cmdline-tools, platform-tools, plateformes 34–37, build-tools — installés le 2026-08-14 pour le premier test réel sur téléphone Android.

## 2026-08-22 (suite) — Point 6 démarré pendant l'attente du test IA Gmail : nom de domaine + CGU/confidentialité
- [x] Rappel programmé (tâche planifiée) pour 21h le 2026-08-22 : vérifier avec Tobie si le scan Gmail IA a bien tourné (voir section précédente).
- [x] Tobie a apporté ses propres recherches sur le coût réel des domaines (PDF partagé, conversation avec un autre assistant) : conclusion correcte — pas de domaine à 1 $/an durable (piège marketing, prix de renouvellement bien plus élevé), coût réel ~10-15 $/an pour un `.com`, recommandation **Porkbun** (le plus simple/transparent) ou **Cloudflare Registrar** (prix coûtant, plus technique), à éviter absolument : GoDaddy/Ionos/1&1 (ventes incitatives, renouvellement opaque).
- [x] Rappelé à Tobie que je ne peux ni créer de compte chez un bureau d'enregistrement ni payer à sa place (règle de sécurité de cette session — création de compte et paiement doivent toujours être faits par lui) — je peux l'accompagner pas à pas au moment de l'achat.
- [x] **Clarification demandée par Tobie ("vérification et indexation par Google")** : ce n'est pas une étape séparée oubliée, c'est un sous-élément du point 6 déjà existant ("validation Google") — Google exige de prouver la propriété du domaine via Google Search Console avant d'accepter de faire passer l'écran de consentement OAuth de Vigie (accès Gmail/Tasks/Calendar) du statut "test" au statut "production". Ordre clarifié : (1) achat du domaine, (2) hébergement d'une page CGU/confidentialité à une adresse fixe, (3) vérification du domaine dans Google Search Console, (4) soumission de l'écran de consentement OAuth pour validation par Google, (5) Play Store.
- [x] **Brouillon des CGU + Politique de confidentialité rédigé** en attendant l'achat du domaine (aucune dépense/compte nécessaire pour cette étape) — couvre notamment la clause "Limited Use" exigée par Google pour l'usage des données Gmail/Tasks/Calendar. Publié en artifact privé pour relecture par Tobie avant toute mise en ligne : à ajuster (notamment l'adresse de contact, en attente du domaine) puis à intégrer au site une fois le domaine actif.
- [x] Relu par Tobie ("ça a l'air bien"), adresse de contact ajustée (`geduldamadou@gmail.com`, provisoire). Question légitime posée : est-ce vraiment "professionnel/légal" ? Réponse honnête donnée : structure standard, correcte pour la vérification Google, mais pas une validation juridique — le point APDP resté ouvert depuis le 2026-08-06 (voir tout début de PROGRESS.md) n'est toujours pas confirmé, à faire relire par un juriste avant un vrai lancement public.
- [x] **Domaine acheté par Tobie lui-même** le 2026-08-23 sur Porkbun : `pentalightcode.com`, 11,08 $/an, prix "At Cost" (pas de piège au renouvellement). Acheté pour PENTALIGHTCODE (l'entreprise), pas pour Vigie seul — décision explicite pour pouvoir héberger plusieurs produits (Vigie, puis SMARTLEARN) sous des sous-domaines (`vigie.pentalightcode.com`, etc.) sans racheter un domaine à chaque fois.
- [x] **Sous-domaine `vigie.pentalightcode.com` connecté à Firebase Hosting** (même site que Vigie, `secretaire-aje`) : domaine personnalisé ajouté dans la console Firebase, enregistrement `CNAME vigie → secretaire-aje.web.app` ajouté dans les DNS Porkbun (le CNAME générique `*.pentalightcode.com → uixie.porkbun.com` déjà présent — laissé tel quel, un enregistrement précis prend toujours le dessus sur un générique, pas besoin de le supprimer). DNS déjà propagé et vérifié (`dig` confirme la résolution) ; certificat SSL Firebase encore en cours de génération au moment de la rédaction (peut prendre de quelques minutes à ~24h) — vérifié via `openssl s_client`, sert encore le certificat générique `firebaseapp.com` en attendant.
- [x] **Page CGU + Politique de confidentialité déployée** sur Firebase Hosting à `/confidentialite/` (fichier source : `app/web/confidentialite/index.html`, copié dans `web/` — donc persistant à travers les futurs `flutter build web`, comme `web/telecharger/Vigie.apk`). Bannière "brouillon interne" retirée avant mise en ligne (texte de fond inchangé). Vérifié en ligne : `https://secretaire-aje.web.app/confidentialite/` répond 200. Apparaîtra automatiquement aussi sur `vigie.pentalightcode.com/confidentialite/` dès que le certificat sera prêt (même contenu, pas de redéploiement nécessaire).
- [x] **Certificat SSL `vigie.pentalightcode.com` prêt** — vérifié via `openssl s_client` (CN=vigie.pentalightcode.com, Google Trust Services, valide jusqu'au 21/11/2026). `https://vigie.pentalightcode.com/confidentialite/` répond 200 sans avertissement de sécurité.
- [x] **Remplacement `secretaire-aje.web.app` → `vigie.pentalightcode.com` (partie sûre)** : `functions/main.py` (`_URL_APK_OFFICIELLE`, message d'erreur, texte de notification, lien de la notif de mise à jour), `scripts/publier_version.py` (`_URL_APK`), `lib/widgets/banniere_mise_a_jour.dart` (texte affiché). Version bumpée `1.15.3+38` → `1.15.4+39`, APK + web buildés et déployés, fonction `publier_version_admin` redéployée, version **1.15.4 (build 39) publiée** (2 utilisateurs notifiés).
- [x] **Lien de connexion (magic link) — traité séparément, à cause du risque** : ce lien dépend des "Authorized domains" de Firebase Auth (sinon la connexion casse). Vérifié que `vigie.pentalightcode.com` y était déjà (Tobie l'avait déjà ajouté, capture d'écran à l'appui — type "Custom" à côté de `localhost`/`secretaire-aje.web.app`/`secretaire-aje.firebaseapp.com`). Une fois confirmé : `lib/services/auth_service.dart` (`ActionCodeSettings.url`) et `android/app/src/main/AndroidManifest.xml` (host de l'intent-filter `/connexion` — attention à ne pas toucher au `pathPrefix`, restriction ajoutée le 17/08 pour ne pas casser les liens de téléchargement/notification, voir commentaire dans le fichier) mis à jour vers `vigie.pentalightcode.com`. Version bumpée `1.15.4+39` → `1.15.5+40`, APK + web buildés et déployés, fonction redéployée, version **1.15.5 (build 40) publiée** (2 utilisateurs notifiés).
- [x] **Testé par Tobie et sa mère, confirmé fonctionnel** : déconnexion/reconnexion par lien magique sans problème sur le nouveau domaine. Le changement le plus sensible de cette phase est validé en conditions réelles.
- [x] `secretaire-aje.web.app` continue techniquement d'exister (Firebase ne permet pas de la désactiver), mais n'est plus référencée nulle part dans le code ni les communications — objectif de Tobie atteint ("plus personne ne la voit ni ne l'utilise").
- [x] **Adresse email d'entreprise activée** : redirection Porkbun confirmée (`contact@pentalightcode.com` → `geduldamadou@gmail.com`, gratuite). §8 Contact de la page CGU/confidentialité mis à jour avec cette adresse (remplace le `geduldamadou@gmail.com` provisoire), redéployé.
- [ ] Ensuite : vérification du domaine dans Google Search Console, puis soumission de l'écran de consentement OAuth à Google pour validation.

## 2026-08-23 (suite) — Bug signalé sur le téléphone du père (Tecno/HiOS) : Agenda "unavailable"
- [x] Tobie a signalé une erreur `[firebase_functions/unavailable]` sur l'écran Agenda, avec capture d'écran — précisé ensuite : téléphone du père, qui est un **Tecno (HiOS)** (correction : ce n'est pas le Redmi/MIUI mentionné plus tôt dans le projet — c'est bien le père qui a le Tecno).
- [x] **Vérifié dans les vrais journaux serveur** (`firebase functions:log`, pas juste les logs de déploiement) : aucune trace de cette requête n'atteint jamais `lister_evenements_calendrier`. Donc pas un bug côté serveur — la requête ne quitte jamais le téléphone.
- [x] Confirmé par Tobie : le problème est **systématique**, pas ponctuel — écarte l'hypothèse d'une simple coupure réseau passagère, pointe vers une restriction du système d'exploitation.
- [x] **Diagnostic donné à Tobie** : même famille de restriction déjà rencontrée sur ce projet (MIUI, notifications/lancement d'app) — probablement le réglage HiOS "Autostart"/"Applications protégées", qui coupe le réseau des apps en arrière-plan et qu'aucune app ne peut demander via une fenêtre système standard (contrairement à l'exemption batterie ajoutée en 1.14.0, une vraie permission Android). Instructions données : Réglages → Applications → Gestion des applications → Vigie → activer "Démarrage automatique" + mettre la batterie sur "Aucune restriction" ; vérifier aussi l'app "Gestionnaire de téléphone" → "Applications protégées".
- [ ] En attente de retour de Tobie après que son père ait ajusté ces réglages, pour confirmer si ça résout le problème.
- [x] **Red Team demandé explicitement par Tobie** : "on est sûr à 100% que le problème ne vient pas de nous depuis l'achat du domaine ?" — audit complet avec preuves (pas juste du raisonnement) :
  - Fichiers réellement modifiés aujourd'hui (vérifié par date de fichier) : `AndroidManifest.xml` (hôte du lien `/connexion` uniquement), `functions/main.py` (URLs dans `publier_version_admin` uniquement), `auth_service.dart`, `banniere_mise_a_jour.dart`, `publier_version.py`, page CGU/confidentialité — aucun ne touche à `lister_evenements_calendrier` ni à Firestore.
  - Vérifié via `firebase functions:log` (horodatage réel, pas les logs de déploiement) : `lister_evenements_calendrier` et `firestore.rules` non modifiés depuis le **21 août**, deux jours avant l'incident. Seule fonction redéployée aujourd'hui : `publier_version_admin`, isolément (jamais de déploiement global).
  - Raison technique : le domaine `pentalightcode.com`/DNS ne concerne que Firebase Hosting (le site web) — les fonctions serveur vivent sur une adresse Google séparée (`cloudfunctions.net`), sans lien technique possible entre les deux.
  - **Conclusion, preuves à l'appui** : rien construit depuis l'achat du domaine ne peut expliquer cette erreur. L'hypothèse HiOS (réglage "Autostart"/"Applications protégées") reste la plus solide.
- [x] **Tobie a poussé plus loin, à raison** : "ce n'est pas pro de demander au daron d'aller bidouiller des réglages, l'app doit fonctionner comme chez tout le monde." Vérifié en direct (WebSearch/WebFetch, pas de mémoire) plutôt que de répéter mon explication précédente : confirmé via [dontkillmyapp.com](https://dontkillmyapp.com/xiaomi) et la [doc officielle Android](https://source.android.com/docs/core/permissions/perms-allowlist) qu'aucune API standard n'existe pour ça — c'est un vrai problème industriel (WhatsApp/Gmail y échappent souvent via des accords d'usine avec les fabricants, pas via une astuce technique disponible à toute app).
- [x] **Compromis pro identifié et implémenté** : bibliothèque [`auto_start_flutter`](https://pub.dev/packages/auto_start_flutter) (utilisée par de vraies apps pour ce problème précis, curatée par marque) ajoutée — `NotificationService._verifierAutostart()` détecte si le téléphone a un écran de démarrage automatique connu et propose une seule fois (jamais à chaque lancement, mémorisé via `SharedPreferences`) une fenêtre "Vérifier ce réglage" qui ouvre directement le bon écran en un tap, au lieu de laisser l'utilisateur chercher lui-même. Honnêteté actée avec Tobie : Tecno/HiOS n'est pas explicitement listé comme marque garantie par cette bibliothèque (contrairement à Xiaomi, Oppo, Infinix...) — amélioration réelle, pas une garantie à 100%, faute d'API officielle Android pour ce cas précis.
- [x] **`auto_start_flutter` a fait échouer la compilation** : le module Android de la bibliothèque n'est pas compatible avec l'AGP 9.0.1 utilisé par ce projet (`NullPointerException` à la configuration Gradle). Annulé proprement (dépendance retirée, code Dart/main.dart nettoyé) avant de laisser un build cassé — `flutter analyze` + `flutter build apk` reconfirmés propres, retour à l'état de la version 1.15.5+40 déjà publiée (rien à redéployer).
- [x] **Tobie a refusé aussi bien "demander à son père" que "le faire moi-même sur son téléphone"** (option 2 explicitement choisie : coder le raccourci nous-mêmes). Recherche complémentaire avant d'écrire du code au hasard : même la bibliothèque de référence de la communauté pour ce problème ([AutoStarter, judemanutd/AutoStarter](https://github.com/judemanutd/AutoStarter)) a des demandes ouvertes et non résolues depuis longtemps pour Tecno/Infinix ([issue #44](https://github.com/judemanutd/AutoStarter/issues/44), [issue #47](https://github.com/judemanutd/AutoStarter/issues/47)) — le mainteneur lui-même dit ne pas avoir ces téléphones pour trouver les bons réglages. Aucun nom d'écran fiable à cibler n'existe pour Tecno/HiOS, dans aucune source. Coder un ciblage deviné aurait eu de fortes chances d'échouer silencieusement.
- [x] **Solution retenue, dans le seul périmètre où le code peut vraiment agir sans réglage de personne** : `GmailService.evenementsCalendrier()` retente automatiquement (jusqu'à 2 fois, délais 1s/3s) sur les erreurs `unavailable`/`deadline-exceeded` avant d'afficher une erreur à l'écran Agenda — absorbe une coupure réseau brève sans aucune action humaine. Honnêteté actée : n'aide que si la coupure est courte (quelques secondes) ; si HiOS bloque le réseau plus longtemps, ça ne suffira pas — c'est la limite du levier purement logiciel sans réglage.
- [x] `flutter analyze` propre, version bumpée `1.15.5+40` → `1.15.6+41`, APK + web buildés et déployés, fonction `publier_version_admin` redéployée, version **1.15.6 (build 41) publiée** (3 utilisateurs notifiés — la mère de Tobie apparaît maintenant aussi, cohérent avec son test de connexion réussi).
- [ ] À observer par Tobie : si l'erreur `[firebase_functions/unavailable]` sur Agenda redevient occasionnelle/disparaît chez le père, la nouvelle tentative automatique suffisait. Si elle persiste de façon identique, ça confirmera un blocage réseau plus long que quelques secondes — dans ce cas, la seule option qui reste est un réglage manuel (unique dans l'industrie pour ce type de restriction, quel que soit le sérieux de l'app).

## 2026-08-24 — Point 6 (suite) : domaine vérifié dans Google Search Console
- [x] Propriété "Domaine" `pentalightcode.com` créée dans Google Search Console, validée par enregistrement DNS TXT (`google-site-verification=...`) ajouté chez Porkbun (host racine). **Validée du premier coup.** Couvre automatiquement tous les sous-domaines, dont `vigie.pentalightcode.com` — pas besoin de revérifier à chaque nouveau produit/sous-domaine plus tard.
- [x] Averti Tobie : ne jamais supprimer cet enregistrement TXT chez Porkbun, sous peine de perdre la validation (avertissement affiché par Google lui-même).
- [x] **Vrai problème de sécurité trouvé en préparant la soumission** : l'écran de consentement OAuth (Google Auth Platform → Accès aux données) déclarait beaucoup plus de scopes que ce que Vigie utilise réellement — notamment `https://mail.google.com/` (accès Gmail complet, y compris suppression définitive d'emails), `.../auth/tasks` et `.../auth/calendar.events` (écriture/modification), en plus de plusieurs scopes Gmail/Calendar inutilisés. Vérifié par `grep` dans le code source (`lib/services/gmail_service.dart`, `functions/main.py`) : Vigie n'utilise **que 3 scopes, tous en lecture seule** — `gmail.readonly`, `tasks.readonly`, `calendar.readonly`. Contradiction directe avec la politique de confidentialité qui promet "lecture seule" — et Google aurait très probablement rejeté la vérification de toute façon (les scopes demandés doivent correspondre à l'usage réel).
- [x] **Corrigé par Tobie dans la console Google** : tous les scopes non utilisés supprimés un par un (poubelle sur chaque ligne), ne restent que `userinfo.email` (connexion de base, non sensible), `gmail.readonly`, `tasks.readonly`, `calendar.readonly`. Sauvegarde confirmée ("Modifications de l'accès aux données enregistrées").
- [x] **App passée en "Production"** (Google Auth Platform → Audience → "Publier l'application" → Confirmer). Conséquence immédiate et attendue : l'écran "Google n'a pas validé cette application" apparaît désormais pour tout le monde, y compris les comptes de test déjà connus — normal, disparaîtra une fois la vérification terminée.
- [x] **Bug de connexion rencontré et résolu** : `GoogleSignInException code 16 (Cancelled by user)` après déconnexion/reconnexion — cause : clic sur "Revenir en lieu sûr" au lieu de "Paramètres avancés" → "Accéder à Vigie (non sécurisé)" sur le nouvel écran d'avertissement. Pas un bug côté app, juste la bonne séquence à suivre (documentée pour la prochaine fois).
- [x] **Page d'accueil publique créée** : Google a détecté que `vigie.pentalightcode.com` (la racine) est l'app elle-même — inaccessible sans connexion, donc rejetée pour le "branding". Nouvelle page statique publique `vigie.pentalightcode.com/apropos/` créée (fichier source `app/web/apropos/index.html`, même persistance que `/confidentialite/` à travers les futurs `flutter build web`) expliquant Vigie sans nécessiter de compte. Champ "Page d'accueil" mis à jour vers cette page. Branding validé et publié avec succès.
- [x] **Vidéo de démonstration enregistrée par Tobie** (obligatoire pour le scope restreint `gmail.readonly`) : connexion Google + écran Agenda + Propositions filmés sur téléphone réel (3 min 33), mise en ligne sur YouTube en "Non répertoriée" : https://youtu.be/FCpGLHWPcEY
- [x] **Justifications rédigées** pour les 3 scopes (calendar.readonly + tasks.readonly ensemble, gmail.readonly séparément) — texte basé sur la vraie logique du code (jamais d'écriture/suppression, propositions à validation manuelle). Fonctionnalité Gmail déclarée : "Surveillance et rapports sur les e-mails".
- [x] **Découverte importante avant l'envoi** : le questionnaire final a révélé que (1) Google déconseille la vérification pour un usage purement personnel (<100 utilisateurs connus) — recommande de rester en mode "Test" dans ce cas, et que (2) demander un scope "restreint" (`gmail.readonly`) impose une **évaluation de sécurité indépendante CASA (Cloud Application Security Assessment)**, généralement payante, à repasser chaque année, avant que l'accès restreint soit définitivement approuvé.
- [x] **Décision explicite de Tobie, en connaissance de cause** : "je veux lancer Vigie sur Play Store, je veux pas faire tout ce travail et le laisser à seulement 2, 3 utilisateurs, je veux pas annuler" — poursuite assumée malgré le coût/la complexité CASA à venir, plutôt que de rester en mode Test limité à la famille.
- [x] **Demande de vérification envoyée à Google** (24/08/2026, ~12h08). Réponses au questionnaire : usage personnel = Non, usage interne = Non, dev/test uniquement = Non, plugin WordPress = Non. Les deux cases de confirmation cochées (conditions lues, CASA compris).
- [ ] **En attente de Google** : examen de la demande (délai typique : plusieurs jours à quelques semaines). Prochaine étape probable : demande de passer l'évaluation CASA pour le scope Gmail restreint — prévoir un budget PENTALIGHTCODE le moment venu. Après validation complète : création du compte développeur Play Store (25 $ une fois), fiche de l'app, publication.

## 2026-08-24 (suite) — Reste de la liste, pas à pas
- [x] **Point 8 (`flutter_secure_storage`) revérifié** : le SDK Android 37 est en fait déjà installé (`android-37.0`), ce n'était plus le blocage. Nouvel essai de mise à jour vers 11.0.0 : révèle un **conflit de dépendances différent** avec `share_plus` (incompatibilité de version `win32`) — nécessiterait de monter `share_plus` vers sa version majeure 13 (changements non testés). Essai annulé proprement (`flutter pub get` + `flutter analyze` reconfirmés propres). Toujours pas urgent, raison mise à jour dans le commentaire du code.
- [x] **Point 5 (retrait du debug temporaire) vérifié puis fait** : Tobie a inspecté `_debug` dans la Console Firebase — un seul document existe (`derniere_erreur`, figé au 14 août, écrasé à chaque nouvelle erreur donc aucune récidive en 10 jours) ; `derniere_erreur_scan`, `derniere_erreur_taches`, `derniere_erreur_calendrier` n'ont **jamais** été créés (zéro erreur sur ces 3 chemins depuis leur ajout). Signal de stabilité net.
- [x] **Code de debug retiré** dans `functions/main.py` (4 sites : `digest_quotidien`, `scanner_emails`, `scanner_taches_google`, `scanner_calendrier_google`) — les écritures Firestore vers `_debug` remplacées par de simples `print()`, déjà le style utilisé ailleurs dans ce fichier (capté automatiquement par `firebase functions:log`, sans avoir besoin d'une collection dédiée). `import main` propre, aucune référence à `_debug` restante. Les 4 fonctions redéployées avec succès.
- [ ] Optionnel, cosmétique : le document `_debug/derniere_erreur` (le seul qui existe) peut être supprimé manuellement dans la Console Firebase si Tobie veut nettoyer — sans risque, plus aucun code ne l'écrit ni ne le lit.
- [ ] Reste sur la liste : point 1 (nettoyage des expéditeurs favoris de test, à faire par Tobie dans l'app), point 7 (carte bancaire Blaze, à faire par Tobie).
